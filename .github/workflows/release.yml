# Publish the frozen runtime as a consumable artifact, per target.
#
# WHY THIS EXISTS. Consumers of this project need a binary they can download,
# start, stop, and call — not a source tree and a recipe. Until now the only way
# to obtain a runtime was to check this repository out at a pinned commit and run
# its PyInstaller spec on your own machines, which made every consumer a builder
# and made the pin a permanent coupling. This workflow ends that: it produces a
# downloadable archive per target, so a consumer needs a version number and
# nothing else.
#
# THE ARTIFACT CONTRACT — the naming is load-bearing, not cosmetic. A consumer
# has to construct these names before the release exists, so they must be
# derivable from (version, target) alone:
#
#   vaultspec-a2a-<version>-x86_64-unknown-linux-gnu.tar.gz   + .sha256
#   vaultspec-a2a-<version>-aarch64-unknown-linux-gnu.tar.gz  + .sha256
#   vaultspec-a2a-<version>-aarch64-apple-darwin.tar.gz       + .sha256
#   vaultspec-a2a-<version>-x86_64-pc-windows-msvc.zip        + .sha256
#
#   <version>  the release tag with its leading `v` removed (v0.2.0 -> 0.2.0)
#   contents   the PyInstaller ONEDIR directory, so extraction yields
#              vaultspec-a2a/vaultspec-a2a  (vaultspec-a2a\vaultspec-a2a.exe on Windows)
#   sidecar    <archive>.sha256, one line, bare lowercase hex, no filename column
#
# The target segment uses Rust target triples even though this is a Python
# project. That is deliberate: the consuming pipeline already keys its support
# matrix and its composition steps on exactly these four strings, so borrowing
# its vocabulary means no translation table sits between producer and consumer.
#
# EVERY TARGET IS BUILT NATIVELY. PyInstaller freezes for the architecture it
# runs on and has no cross-compilation flag, so a target built elsewhere would
# ship the wrong interpreter inside a correctly-named archive — which composes
# cleanly, verifies cleanly, and fails only on a user's machine. All four
# runners below are self-hosted fleet runners and native, including the
# Linux/ARM64 leg.
name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing release tag to build and upload (e.g. v0.2.0)"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-${{ github.workflow }}-${{ inputs.tag || github.ref }}
  cancel-in-progress: false

jobs:
  health:
    name: "Check: Release qualification"
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read
    with:
      ref: ${{ inputs.tag || github.ref }}

  build:
    name: Freeze ${{ matrix.target }}
    needs: health
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: [self-hosted, Linux, X64]
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            runner: [self-hosted, ARM64, Linux]
            archive: tar.gz
          - target: aarch64-apple-darwin
            runner: [self-hosted, macOS, ARM64]
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            runner: [self-hosted, Windows, X64]
            archive: zip
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
          persist-credentials: false

      - name: Set up uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          # `false` on purpose, and it is not a de-optimisation. These runners
          # are self-hosted and persistent, and each one pins UV_CACHE_DIR at a
          # cache shared by every runner on its host - so the cache is already
          # warm before the job starts and survives after it ends. `true` adds a
          # GitHub-cache round trip on top of that, and its post step then tries
          # to upload the whole shared directory: measured on this branch, the
          # `Post Set up uv` step ran for 18 minutes and was still going when it
          # hit the job's 20-minute budget, cancelling a run whose actual work
          # had finished in 81 seconds. Every sibling repo on this fleet already
          # calls setup-uv without it.
          enable-cache: false
          version: "0.11.29"

      - name: Install Python
        run: uv python install 3.13

      # Resolve a host interpreter ONCE, by name, and fail loudly if there is
      # none. The steps below parse JSON with it, and `python3` is not a reliable
      # name on the Windows runner's bash — a bare `python3` there would fail
      # mid-script with "command not found" rather than saying what was missing.
      - name: Resolve a host Python for the release scripts
        shell: bash
        run: |
          set -euo pipefail
          for candidate in python3 python; do
            if command -v "$candidate" >/dev/null 2>&1; then
              echo "PY=$candidate" >> "$GITHUB_ENV"
              echo "using $candidate: $("$candidate" --version)"
              exit 0
            fi
          done
          echo "::error::neither python3 nor python is on PATH; the release scripts cannot parse JSON" >&2
          exit 1

      # The archive name must match the code inside it. A tag and a
      # `[project] version` that disagree would publish an archive whose name
      # promises one version and whose binary reports another — refuse rather
      # than pick a winner.
      - name: Resolve and agree the version
        id: version
        shell: bash
        env:
          RAW_TAG: ${{ inputs.tag || github.ref_name }}
          MANUAL_TAG: ${{ inputs.tag }}
          EVENT_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          if [[ ! "$RAW_TAG" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            echo "::error::release tag must be strict vMAJOR.MINOR.PATCH" >&2
            exit 1
          fi
          if [ -z "$MANUAL_TAG" ] && [ "$EVENT_REF" != "refs/tags/$RAW_TAG" ]; then
            echo "::error::automatic publication requires an exact tag ref" >&2
            exit 1
          fi
          tag_ref="refs/tags/$RAW_TAG"
          git show-ref --verify --quiet "$tag_ref" || {
            echo "::error::release tag does not exist: $RAW_TAG" >&2
            exit 1
          }
          tag_commit="$(git rev-parse "$tag_ref^{}")"
          head_commit="$(git rev-parse HEAD)"
          if [ "$tag_commit" != "$head_commit" ]; then
            echo "::error::release tag $RAW_TAG does not resolve to checked-out HEAD" >&2
            exit 1
          fi
          tag_version="${RAW_TAG#v}"
          project_version="$($PY -c 'import tomllib,pathlib; print(tomllib.loads(pathlib.Path("pyproject.toml").read_text())["project"]["version"])')"
          echo "tag=${tag_version} pyproject=${project_version}"
          if [ "${tag_version}" != "${project_version}" ]; then
            echo "::error::tag version '${tag_version}' does not match pyproject [project] version '${project_version}'; the archive name would contradict the binary it contains" >&2
            exit 1
          fi
          echo "value=${tag_version}" >> "$GITHUB_OUTPUT"

      - name: Refuse to freeze for an architecture this runner is not
        # PyInstaller bundles the interpreter it is RUNNING on. The matrix above
        # already pairs each target with a native runner; this proves the pairing
        # held rather than assuming it, because a mislabelled archive is caught by
        # nothing downstream — the bytes are internally consistent and only the
        # name lies.
        shell: bash
        run: |
          set -euo pipefail
          case "${{ matrix.target }}" in
            aarch64-*) want=arm64 ;;
            x86_64-*)  want=x64 ;;
            *) echo "::error::unhandled target ${{ matrix.target }}" >&2; exit 1 ;;
          esac
          case "$(uname -m)" in
            aarch64|arm64) have=arm64 ;;
            x86_64|amd64)  have=x64 ;;
            *) echo "::error::unrecognised machine $(uname -m) for ${{ matrix.target }}" >&2; exit 1 ;;
          esac
          if [ "$want" != "$have" ]; then
            echo "::error::${{ matrix.target }} needs a $want runner; this one is $have. The frozen runtime cannot be cross-compiled." >&2
            exit 1
          fi
          echo "runner architecture $have matches ${{ matrix.target }}"

      - name: Install the locked freeze environment
        run: uv sync --locked --no-default-groups --group freeze

      - name: Freeze the runtime
        shell: bash
        run: uv run --no-sync --frozen python scripts/build_binary.py --dist dist/binary

      # THE TREE THE CONSUMER ACTUALLY ACCEPTS. The archive is unpacked into an
      # immutable generation tree, and that tree's rules are narrower than "a
      # directory of files": it admits no directory without a regular-file
      # descendant, and no link or reparse object anywhere. A tree that breaks
      # either is refused at INSTALL time, on a user's machine, long after this
      # workflow went green — and the consumer names the freeze recipe as the
      # thing at fault, because it is.
      #
      # This is cheap and it is checked here rather than trusted, because what
      # populates the onedir is PyInstaller plus whatever the dependency set
      # happens to lay down: a bump can introduce an empty package directory or
      # a symlinked shared library without anything in this repository changing.
      - name: Prove the frozen tree is one the consumer can install
        shell: bash
        run: |
          set -euo pipefail
          "$PY" - <<'PY'
          import os
          import stat
          import sys
          from pathlib import Path

          root = Path("dist/binary/vaultspec-a2a")
          if not root.is_dir():
              sys.exit(f"::error::frozen onedir absent at {root}")

          REPARSE = 0x400  # FILE_ATTRIBUTE_REPARSE_POINT
          empty: list[str] = []
          nonregular: list[str] = []
          links: list[str] = []
          directories: set[Path] = set()
          populated: set[Path] = set()
          files = 0


          def is_link(entry: Path) -> bool:
              """A junction is not a symlink, and both are reparse objects."""
              attributes = getattr(entry.lstat(), "st_file_attributes", 0)
              return bool(attributes & REPARSE) or entry.is_symlink()


          for dirpath, dirnames, filenames in os.walk(root):
              directory = Path(dirpath)
              directories.add(directory)
              regular = 0
              for name in filenames:
                  entry = directory / name
                  if is_link(entry):
                      links.append(str(entry.relative_to(root)))
                  elif stat.S_ISREG(entry.lstat().st_mode):
                      regular += 1
                  else:
                      nonregular.append(str(entry.relative_to(root)))
              files += regular
              if regular:
                  # "No regular-file DESCENDANT" is the rule, so a directory
                  # holding only populated subdirectories is legitimate: credit
                  # every ancestor rather than re-walking each subtree.
                  for ancestor in (directory, *directory.parents):
                      populated.add(ancestor)
                      if ancestor == root:
                          break
              # Never DESCEND through a reparse object. os.walk does not treat a
              # Windows junction as a link, so a self-referential one would walk
              # forever; recording it and pruning it is what keeps this bounded.
              kept = []
              for name in dirnames:
                  if is_link(directory / name):
                      links.append(str((directory / name).relative_to(root)))
                  else:
                      kept.append(name)
              dirnames[:] = kept

          empty = sorted(str(d.relative_to(root)) for d in directories - populated)

          for label, found in (
              ("directory with no regular-file descendant", empty),
              ("link or reparse object", links),
              ("non-regular entry", nonregular),
          ):
              for path in found[:20]:
                  print(f"::error::frozen tree contains a {label}: {path}")
              if len(found) > 20:
                  print(f"::error::...and {len(found) - 20} more of: {label}")

          if empty or links or nonregular:
              sys.exit(
                  "the frozen tree violates the consumer's generation-tree rules; "
                  "it would be refused when installed, not here"
              )
          print(f"frozen tree accepted: {files} files, no empty subtree, no links")
          PY

      # THE GATE THAT MATTERS. The build script's own smoke proves the frozen
      # dispatch surface parses — `--version`, `--help`, and a run-module refusal
      # — but it never boots anything, so it cannot prove the artifact is USABLE.
      # A consumer's requirement is "start it, call its API, stop it", and that is
      # what is asserted here against the real frozen binary.
      #
      # Two details this gate is deliberate about:
      #   * `--port` is passed explicitly. The default (18000) is FIXED, not
      #     ephemeral, so relying on it would make two concurrent jobs on one
      #     machine collide silently.
      #   * The pid is checked directly after `stop`. A clean exit from a stop
      #     verb proves the verb returned, not that the process tree died.
      - name: Prove the artifact starts, serves, and stops
        shell: bash
        timeout-minutes: 6
        env:
          SMOKE_PORT: "18873"
          TARGET: ${{ matrix.target }}
        run: bash scripts/prove_artifact_lifecycle.sh

      - name: Archive the onedir
        id: archive
        shell: bash
        run: |
          set -euo pipefail
          version="${{ steps.version.outputs.value }}"
          name="vaultspec-a2a-${version}-${{ matrix.target }}"
          if [ "${{ matrix.archive }}" = "zip" ]; then
            archive="${name}.zip"
            ( cd dist/binary && 7z a -tzip -bso0 "../../${archive}" vaultspec-a2a >/dev/null )
          else
            archive="${name}.tar.gz"
            tar -czf "${archive}" -C dist/binary vaultspec-a2a
          fi
          # Bare lowercase digest, no filename column: the consumer compares it
          # against its own computed digest, and a second field would have to be
          # stripped by every reader.
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${archive}" | cut -d' ' -f1 > "${archive}.sha256"
          else
            shasum -a 256 "${archive}" | cut -d' ' -f1 > "${archive}.sha256"
          fi
          echo "${archive}: $(cat "${archive}.sha256")"
          echo "name=${archive}" >> "$GITHUB_OUTPUT"

      - name: Upload the built member
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-${{ matrix.target }}
          path: |
            ${{ steps.archive.outputs.name }}
            ${{ steps.archive.outputs.name }}.sha256
          if-no-files-found: error
          retention-days: 7

  provenance:
    name: "Build: Release provenance (Linux)"
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
      attestations: write
    outputs:
      count: ${{ steps.subjects.outputs.count }}
      names: ${{ steps.subjects.outputs.names }}
    steps:
      - name: Collect every built member
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: release-*
          path: release-members
          merge-multiple: true

      - name: Enumerate immutable archives
        id: subjects
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t subjects < <(
            find release-members -maxdepth 1 -type f ! -name '*.sha256' | LC_ALL=C sort
          )
          if [ "${#subjects[@]}" -ne 4 ]; then
            echo "::error::expected four release archives to attest; found ${#subjects[@]}" >&2
            exit 1
          fi
          {
            echo "count=${#subjects[@]}"
            echo "paths<<SUBJECTS_EOF"
            printf '%s\n' "${subjects[@]}"
            echo "SUBJECTS_EOF"
            echo "names<<NAMES_EOF"
            printf '%s\n' "${subjects[@]}" | xargs -n 1 basename
            echo "NAMES_EOF"
          } >> "$GITHUB_OUTPUT"

      # Repository/workflow provenance only. This is not Authenticode,
      # Apple code-signing, notarization, or an operating-system trust grant.
      - name: Attest the release archives
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-path: ${{ steps.subjects.outputs.paths }}

  publish:
    name: Publish to the release
    needs: [build, provenance]
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - name: Collect every built member
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: ${{ runner.temp }}/release-members-${{ github.run_id }}-${{ github.run_attempt }}
          merge-multiple: true

      # The cohort is all four or none. Uploading a partial set would publish a
      # release that looks complete to anyone who only checks their own platform,
      # and the missing target would surface as a download 404 much later.
      - name: Refuse an incomplete cohort
        shell: bash
        env:
          RAW_TAG: ${{ inputs.tag || github.ref_name }}
          MEMBERS_DIR: ${{ runner.temp }}/release-members-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          set -euo pipefail
          version="${RAW_TAG#v}"
          expected=()
          for spec in \
            "x86_64-unknown-linux-gnu:tar.gz" \
            "aarch64-unknown-linux-gnu:tar.gz" \
            "aarch64-apple-darwin:tar.gz" \
            "x86_64-pc-windows-msvc:zip"; do
            target="${spec%%:*}"; ext="${spec##*:}"
            archive="$MEMBERS_DIR/vaultspec-a2a-${version}-${target}.${ext}"
            expected+=("$archive" "$archive.sha256")
          done
          for member in "${expected[@]}"; do
            [ -f "$member" ] && [ ! -L "$member" ] || {
              echo "::error::missing or non-regular release member: $member" >&2
              exit 1
            }
          done
          for archive in "${expected[@]}"; do
            [[ "$archive" == *.sha256 ]] && continue
            expected_digest="$(tr -d '\r\n' < "$archive.sha256")"
            actual_digest="$(sha256sum "$archive" | cut -d' ' -f1)"
            if [[ ! "$expected_digest" =~ ^[0-9a-f]{64}$ ]] || [ "$actual_digest" != "$expected_digest" ]; then
              echo "::error::checksum mismatch for $archive" >&2
              exit 1
            fi
          done
          mapfile -d '' actual < <(find "$MEMBERS_DIR" -mindepth 1 -maxdepth 1 -print0)
          if [ "${#actual[@]}" -ne "${#expected[@]}" ]; then
            echo "::error::release cohort contains unexpected members" >&2
            printf '::error::member: %s\n' "${actual[@]}" >&2
            exit 1
          fi
          printf 'accepted release member: %s\n' "${expected[@]}"

      - name: Upload the assets to the release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RAW_TAG: ${{ inputs.tag || github.ref_name }}
          MEMBERS_DIR: ${{ runner.temp }}/release-members-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          set -euo pipefail
          if ! command -v gh >/dev/null 2>&1; then
            echo "::error::gh is required to upload release assets and is not on PATH" >&2
            exit 1
          fi
          version="${RAW_TAG#v}"
          members=(
            "$MEMBERS_DIR/vaultspec-a2a-${version}-x86_64-unknown-linux-gnu.tar.gz"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-x86_64-unknown-linux-gnu.tar.gz.sha256"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-aarch64-unknown-linux-gnu.tar.gz"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-aarch64-unknown-linux-gnu.tar.gz.sha256"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-aarch64-apple-darwin.tar.gz"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-aarch64-apple-darwin.tar.gz.sha256"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-x86_64-pc-windows-msvc.zip"
            "$MEMBERS_DIR/vaultspec-a2a-${version}-x86_64-pc-windows-msvc.zip.sha256"
          )
          gh release upload "$RAW_TAG" "${members[@]}" --clobber --repo "$GITHUB_REPOSITORY"
          echo "uploaded ${#members[@]} assets to $RAW_TAG"

      - name: Verify attached archive provenance
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RAW_TAG: ${{ inputs.tag || github.ref_name }}
          ATTESTED: ${{ needs.provenance.outputs.names }}
          EXPECTED: ${{ needs.provenance.outputs.count }}
          PUBLISHED_DIR: ${{ runner.temp }}/published-release-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          set -euo pipefail
          if ! gh attestation verify --help 2>&1 | grep -q -- '--signer-workflow'; then
            echo "::error::this runner's gh cannot pin an attestation signer workflow" >&2
            exit 1
          fi
          mkdir -p "$PUBLISHED_DIR"
          version="${RAW_TAG#v}"
          archives=(
            "vaultspec-a2a-${version}-x86_64-unknown-linux-gnu.tar.gz"
            "vaultspec-a2a-${version}-aarch64-unknown-linux-gnu.tar.gz"
            "vaultspec-a2a-${version}-aarch64-apple-darwin.tar.gz"
            "vaultspec-a2a-${version}-x86_64-pc-windows-msvc.zip"
          )
          for archive in "${archives[@]}"; do
            gh release download "$RAW_TAG" --repo "$GITHUB_REPOSITORY" \
              --pattern "$archive" --dir "$PUBLISHED_DIR" --clobber
          done
          attached_names="$(printf '%s\n' "${archives[@]}" | LC_ALL=C sort)"
          attested_names="$(printf '%s\n' "$ATTESTED" | sed '/^[[:space:]]*$/d' | LC_ALL=C sort)"
          if [ "$attached_names" != "$attested_names" ] || [ "${#archives[@]}" -ne "$EXPECTED" ]; then
            echo "::error::the attached archive set differs from the attested set" >&2
            exit 1
          fi
          for archive in "${archives[@]}"; do
            gh attestation verify "$PUBLISHED_DIR/$archive" \
              --repo "$GITHUB_REPOSITORY" \
              --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/release.yml" \
              < /dev/null
          done

      - name: Publish the complete release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RAW_TAG: ${{ inputs.tag || github.ref_name }}
        run: gh release edit "$RAW_TAG" --draft=false --repo "$GITHUB_REPOSITORY"
