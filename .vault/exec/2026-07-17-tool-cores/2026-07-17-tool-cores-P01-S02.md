---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S02'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Re-express the researcher persona to name the native Read, Grep, and Glob grounding tools and remove the terminal-false-unexecutable vaultspec-core and rag CLI invocations, claiming P03.S05 of the graph-agent-framework-harness plan

## Scope

- `the rag MCP tool names are added later once surfacing is confirmed (executor-service)`
- `src/vaultspec_a2a/team/presets/agents/vaultspec-researcher.toml`

## Description

- Rewrite the researcher persona's Discovery sequence in `vaultspec-researcher.toml` to instruct grounding via the native `Read`, `Grep`, and `Glob` tools only, which run over the workspace filesystem agent-side.
- Remove the four terminal-unexecutable CLI invocations the persona previously named (`vaultspec-core status`, two `vaultspec-rag search` calls, and `rg`), which the persona's `terminal = false` capability can never run.
- State the grounding order explicitly: Glob and Read the vault decision records (`.vault/adr/`, `.vault/research/`, `.vault/audit/`) first, then Grep the codebase for implementation sites, then Read the epicenter file whole and confirm symbols with a targeted Grep.

## Outcome

The researcher persona now names only tools the model can actually see and invoke, closing the prompt-capability drift for this role. The persona voice, structure, findings format, and quality bar are preserved; the not-a-writer prohibitions (no propose/validate/request_apply, no filesystem writes) are left intact. No `vaultspec-rag` MCP tool name is added here — that is deferred to P03.S15, gated on the P02 surfacing re-probe.

## Notes

Scope held to the researcher preset only; synthesist, adr-author, and doc-reviewer carry no non-executable instructions (already checked under the graph-agent-framework-harness plan). The persona parses as valid TOML and still reports `role = researcher` with `terminal = false`.
