---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S03'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Correct the falsified stdio-surfaces-reliably docstring to the S20 registration-scope truth (executor-core)

## Scope

- `src/vaultspec_a2a/graph/nodes/worker.py`

## Description

- Correct the `_attach_authoring_tools` docstring in `worker.py` that asserted the pinned CLI "surfaces stdio MCP tools reliably while it connects to but does not surface loopback HTTP MCP tools".
- Replace it with the S20 registration-scope truth: on the pinned stack, session-injected MCP servers do not surface to the model over either transport (only user-global home-config servers surface; session-injected and workspace-config connect and serve but never surface, stdio and HTTP alike), so transport choice is not a surfacing lever.
- Re-express the transport-selection sentence as a plain field statement (stdio when the binding carries `engine_base_url` + `run_id`, otherwise HTTP) rather than a surfacing-benefit claim.

## Outcome

The docstring now states the constraint, not the superseded transport hypothesis from the 2026-07-14 R4 amendment. Behavior is unchanged — this is a documentation-truth correction only; the worker still prefers the stdio bridge when the engine transport is present.

## Notes

The identical falsified claim in `_acp_authoring.py` is corrected under P01.S04. One further occurrence of the "surfaces reliably" framing survives in `authoring_stdio.py` (module docstring), outside this step's and P01.S04's named scope; flagged for a follow-up rather than corrected here to respect step boundaries. A test docstring in `test_worker_authoring_wiring.py` also restates the old framing; left untouched (test-file, out of scope) and reported.
