---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S04'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Correct the falsified stdio-surfaces-reliably docstring to the S20 registration-scope truth (executor-core)

## Scope

- `src/vaultspec_a2a/providers/_acp_authoring.py`

## Description

- Correct all three "stdio surfaces reliably" assertions in `_acp_authoring.py`: the `AUTHORING_STDIO_MODULE` comment ("the surfacing-reliable transport"), the `AuthoringToolBinding` class docstring ("stdio preferred while the CLI defers HTTP MCP tool surfacing"), and the `build_authoring_stdio_mcp_servers` docstring ("the transport the CLI surfaces reliably (R4 amendment)").
- Replace each with the S20 registration-scope truth: neither transport surfaces a session-injected server to the model on the pinned stack; only user-global home-config servers surface. The stdio shape is a spawn/wiring mechanism, not a surfacing one.

## Outcome

The `_acp_authoring.py` file no longer contradicts its own S20 evidence. Behavior is unchanged — documentation-truth correction only. Together with P01.S03 the two named files carry the corrected constraint consistently.

## Notes

The assignment named lines ~104 and ~113; the same falsified claim also lived at the module comment (~73) and the stdio-builder docstring (~241) in the same file, so all three in-file occurrences were corrected to avoid leaving the file self-contradictory. A residual occurrence remains in `authoring_stdio.py` (a different file, unnamed by any P01 step) and is flagged for follow-up rather than corrected here.

Grounding: shared the S20 registration-scope evidence with P01.S03 (governing `2026-07-17-tool-cores-adr` Considerations and `2026-07-17-tool-cores-research`); the same Grep sweep over `src/vaultspec_a2a` located all three in-file occurrences (~73, ~113, ~241) plus the out-of-scope `authoring_stdio.py` residual. (This step predates the standing rag-cite directive; the rag service was unavailable this session to attach literal query citations.)
