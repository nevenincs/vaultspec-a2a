---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S12'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Declare the team.harness mcp_servers opt-in naming vaultspec-rag on the live document-authoring preset (executor-service)

## Scope

- `src/vaultspec_a2a/team/presets/teams/vaultspec-adr-research.toml`

## Description

- Add a `[team.harness]` block to the live `vaultspec-adr-research.toml` preset declaring `mcp_servers = ["vaultspec-rag"]`, opting every document-authoring worker into the read-only vaultspec-rag grounding server.
- Add a deterministic test in `TestAdrResearchTeamPreset` that loads the real preset and asserts `effective_harness().mcp_servers == ["vaultspec-rag"]` with the default five authoring surfaces intact (no mocks, real TOML).

## Outcome

The live preset now carries the `[team.harness]` opt-in that makes the landed grounding composition (P03.S10 allowlist, S11 worker threading, S13/S14 isolated config home) effective for its writer roles: `TeamConfig.effective_harness()` returns the declared block verbatim with `vaultspec-rag` in `mcp_servers`, so the composed tool names join the autonomous allowedTools and the stdio server is advertised in `session/new`. Read-only by construction - `vaultspec-rag` is the sole `_KNOWN_MCP_SERVERS` entry and no write-capable server is composed. `required_surfaces` defaults to all five authoring surfaces, so the opt-in only WIDENS the harness (no authoring-surface regression). The synthesist, adr-author, and doc-reviewer inherit the team-level opt-in.

Grounding: Read `TeamConfig.effective_harness` and the `TeamHarnessConfig` schema (`team_config.py`, the `mcp_servers: list[str]` field) for the exact opt-in shape, and the `_KNOWN_MCP_SERVERS` registry (`_acp_mcp.py`) to confirm the server name `vaultspec-rag`; modelled the test on the existing `TestAdrResearchTeamPreset` real-preset-loading pattern. (rag service was unavailable this session for literal query citations; grounding was Read/Grep-led over these well-named targets.)

## Notes

Gate: ruff + ty clean; `test_team_config.py` 111 passed (incl. the new opt-in test); graph harness-wiring + compiler + research_adr suites 38 passed (the harness opt-in activates vaultspec-rag composition for the live preset without regressing graph compilation).

SEQUENCING CAVEAT: this branch (`tool-cores-p03s`) lands ONLY AFTER `tool-cores-p03` - executor-core's composition/allowlist wiring plus the compiler.py parity fix and hardenings are what make this opt-in effective at run time. Declaring the opt-in before that wiring lands would activate composition against an incomplete seam. Hold for the team lead's land sequencing; do not merge ahead of p03.
