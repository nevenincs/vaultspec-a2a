---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S15'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Add the rag search MCP tool name to the researcher persona grounding instructions once surfacing is confirmed by the P02 outcome (executor-service)

## Scope

- `src/vaultspec_a2a/team/presets/agents/vaultspec-researcher.toml`

## Description

- Re-express the researcher persona's Discovery sequence in `vaultspec-researcher.toml` to name the surfaced rag MCP tools - `mcp__vaultspec-rag__search_vault`, `mcp__vaultspec-rag__search_codebase`, `mcp__vaultspec-rag__get_code_file` - for semantic grounding, alongside the native Read/Grep/Glob added in P01.S02.
- Lead the sequence by MEANING (rag semantic search of vault then code) and confirm with the exact-match native tools (Read the epicenter whole, targeted Grep), matching the project discovery philosophy.

## Outcome

The researcher now instructs semantic recall via the read-only rag tools the model can actually see (surfacing confirmed by the P02 re-probe: S14 verdict SURFACES), on top of the deterministic native-read floor. The no-CLI / no-terminal prohibition is preserved intact - the persona still names no `vaultspec-core`/`vaultspec-rag` shell command and keeps `terminal = false` (verified); voice, findings format, and quality bar unchanged. The three named tools map to the vaultspec-rag server declared for this team in P03.S12.

Grounding: took the exact surfaced tool names from the team lead's S14-verdict handoff (`search_vault`/`search_codebase`/`get_code_file` under `mcp__vaultspec-rag__`); Read the current persona (post-P01.S02) to extend its discovery sequence in place. (rag service unavailable this session for literal query citations; grounding was Read-led over the named persona.)

## Notes

Gate: persona parses as valid TOML with `role = researcher`, `terminal = false`; the team preset/persona test set passed (27 selected). No code path changed - this is a prompt-capability edit.

SEQUENCING CAVEAT: gated on the P03.S14 surfacing verdict (SURFACES) and lands with `tool-cores-p03s` ONLY AFTER `tool-cores-p03`; naming tools the runtime cannot yet surface would reintroduce the persona-drift this step closes. Hold for the team lead's land sequencing.
