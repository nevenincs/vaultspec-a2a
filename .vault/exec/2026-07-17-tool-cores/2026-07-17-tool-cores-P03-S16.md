---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S16'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Prove live that a Claude document agent invokes vaultspec-rag search mid-turn, capturing the tool-call trace and confirming citations resolve to real locations, real run with no mocks and zero .vault writes (executor-service)

## Scope

- `src/vaultspec_a2a/service_tests/`

## Description

- Arm the Claude-lane variant of the semantic-tier proof: the SAME test `test_document_agent_invokes_rag_search_midturn_and_cites` (`test_tool_cores_floor_live.py`), profile-selected via `S05_PROFILE` (default `team-defaults` = the Claude lane), so S16 is a parameter swap of the S17 Z.ai proof.
- No separate driver: Z.ai and Claude ride the identical `AcpChatModel`/adapter (`factory.py`); the harness, SSE observation, rag-invocation assertion, citation resolution, and zero-writes watcher are shared.

## Outcome

ARMED and runnable; the Claude-lane live evidence is BLOCKED on the Claude weekly usage window (~re-arm 2026-07-20 08:00), the same gate as P01.S05. The harness is empirically validated GREEN on the Z.ai lane (P03.S17, run `pw7-1784282060`), which rides the identical code path, so the Claude run is a parameter swap once usage is unblocked. Not reported as passing until that run exists; the S16 checkbox stays OPEN.

## Notes

Re-arm (one command, with the stack up per the S17 recipe and Claude usage unblocked): restore rag discovery + index the engine-scoped workspace on `:8766`, then run `pytest -m service -k invokes_rag_search` on the default profile; capture the run id, the agent's `mcp__vaultspec-rag__search_*` invocation, the resolving citations, the daemon `POST /search` corroboration, and the empty document-dir write-delta. Pre-flight and the boot recipe are recorded in the P03.S17 exec record. Shares the harness + the engine/rag pre-flight with S17; the only difference is the profile (Claude vs Z.ai).
