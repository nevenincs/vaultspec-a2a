---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S20'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Prove live on the Codex lane that a document agent reads a named .vault ADR via read-only sandbox filesystem access mid-turn and cites it, real run with no mocks and zero .vault writes (executor-service)

## Scope

- `src/vaultspec_a2a/service_tests/`

## Description

- Drive the Codex floor proof through the SAME harness as S05/S17 (`test_document_agent_reads_named_adr_midturn_and_cites`), profile-selected via `S05_PROFILE` - no new driver. A temporary all-Codex profile (`codex-all`, reverted, not committed) routes every role to Codex so no role gates on Claude.
- Boot the engine scoped to the seeded workspace (real `.vault` ADRs) + gateway per the S05 recipe; Codex auth is file-based (`~/.codex/auth.json`), auto-copied into the per-run `CODEX_HOME` by the landed P04 code.

## Outcome

BLOCKED - not proven, not faked. The Codex/ChatGPT account is USAGE-LIMITED. A direct `CodexChatModel` probe (factory-built, real app-server) returned the app-server error verbatim: "You've hit your usage limit. ... try again at Jul 23rd, 2026 6:15 AM." The live run `pw7-1784282602` terminated in ~7s with no agent output for the same reason. The Codex CLI (`codex-cli 0.144.5`) and auth resolve fine (`auth_mode: chatgpt`, refreshed 2026-07-17T08:18Z; `probe_provider_readiness(CODEX)` ready) - the block is purely metered usage, not a code or credential defect.

The floor path itself needs no harness wiring (native read-only sandbox fs read), so S20 is expected to pass once usage returns - a parameter swap of the S05/S17 harness on the `codex-all` profile.

## Notes

Re-arm (~2026-07-23 06:15 local, when the Codex usage window resets): boot the engine (scoped to a workspace carrying real `.vault` ADRs) + gateway per the S05 recipe, then run `pytest -m service -k reads_named_adr` with `S05_PROFILE=codex-all` (temp all-Codex profile) and Codex auth live; capture the run id, the agent's message_chunk citation + distinctive interior token, and the empty document-dir write-delta. Shares the usage gate with P03.S16 and the Claude S05, and the harness with P04.S21 (which additionally carries a wiring gap - see that record). No code committed for S20: it reuses the existing floor test.
