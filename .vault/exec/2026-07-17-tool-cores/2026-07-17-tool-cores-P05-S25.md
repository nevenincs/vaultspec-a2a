---
tags:
  - '#exec'
  - '#tool-cores'
date: '2026-07-17'
modified: '2026-07-17'
step_id: 'S25'
related:
  - "[[2026-07-17-tool-cores-plan]]"
---

# Reconcile the plan and exec records against what actually landed, ensuring every Step has its exec record and the Verification criteria are honestly closed (executor-service)

## Scope

- `.vault/exec/`

## Description

- Reconcile the tool-cores plan and exec records against what actually landed: verify every closed step has a canonical exec record and every open step records an honest re-arm; record the dispatch deviations; and check the plan Verification criteria against reality.

## Outcome

### Step / exec-record reconciliation

All 25 steps now have a canonical exec record under `.vault/exec/2026-07-17-tool-cores/` (`...-P0N-SNN.md`). Twenty-one steps are closed with records: `P01.S01-S04`, `P02.S06-S09`, `P03.S10-S15`, `P03.S17`, `P04.S18-S19`, `P05.S22-S24`. Two exec-record gaps were fixed during this pass via `vault add exec` - `P05.S23` (its deliverable is the dedup audit `2026-07-17-tool-cores-dedup-audit`) and `P05.S24` (its deliverable is the review audit `2026-07-17-tool-cores-audit`, STATUS PASS) - which had produced their audit artifacts but no exec-dir record at execution time. Four steps remain OPEN BY DESIGN, each recording an honest re-arm, not a completion: `P01.S05` and `P03.S16` (Claude, usage window ~2026-07-20 08:00), `P04.S20` and `P04.S21` (Codex, usage window ~2026-07-23 06:15; S21 additionally gated on the harness-wiring fix, now landed on the codexwire branch). This step, `P05.S25`, is complete when this reconciliation lands even though those four rows stay open.

### Dispatch deviations from the plan-as-written

1. Persona assignment vs actual executor: the plan rows tag `P01.S01`/`S03`/`S04` as executor-core, but the dispatch gave ALL of P01 to executor-service to avoid worktree contention (one owner per file: `worker.py` and `_acp_authoring.py` were touched across S01/S03/S04).
2. `P02.S09` is tagged executor-service in-plan but was executed by executor-core.
3. Commit `e50b88e`'s message says "20 steps" but the committed plan carried the canonical 25-step version (the plan rebuild landed between review and commit; the row count is 25, the message text is stale).
4. Scope extensions, both disclosed: `P03.S11`'s file scope extended to `compiler.py` (parity fix for the worker composition site), and `P04.S18`'s to the codexwire harness-threading fix (a defect discovered by the `P04.S21` live proof - the preset harness servers were never threaded into `CodexChatModel`, so the emitted config.toml was always empty; fix dispatched to executor-core).

### Plan Verification criteria vs reality

- Floor (native read grounding): Z.ai lane PROVEN live green (run `pw7-1784274009`); Claude lane armed, usage-gated (`P01.S05`). Codex floor armed, usage-gated (`P04.S20`).
- Adapter migration: VERIFIED - the regression surface (`session/new`, permission modes/`allowedTools`, `mcpServers` key, capability flags, fs-RPC) confirmed intact on `@agentclientprotocol/claude-agent-acp@0.59.0`, and the S20 re-probe outcome (NOT SURFACED for session-injection) recorded as the `P02.S09` exec record - which routed P03 to the isolated-config-home surfacing path.
- Semantic tier (vaultspec-rag): Z.ai PROVEN live green (run `pw7-1784282060`) with the un-fakeable `:8766` daemon `POST /search` access-log corroboration and resolving citations; Claude armed (`P03.S16`); Codex (`P04.S21`) armed after the wiring fix + usage window.
- Read-only discipline: ENFORCED and AUDITED - no write verb in any composed surface (rag `tools` are read-only; write verbs omitted; the config-home refuses non-read-only entries fail-loud), and zero agent-origin `.vault` document writes across every proof run; the `P05.S24` review PASS confirms it.
- Codex undocumented-axis admission (`approval_policy:never` + `sandbox:read-only` + per-tool `approval_mode`): NOT YET VERIFIED - gated on the Codex usage window and the harness-wiring fix; recorded as the `P04.S21` re-arm, not reported as passing.
- Close-out: reviewer PASS obtained (`P05.S24`); this record is the reconciled-exec-record-for-every-step criterion.

## Notes

Reconciliation authored directly in main (markdown-only; vault single-writer). Main carried a parallel session's WIP (`control/health.py`, `control/worker_management.py`, and untracked files) which was NOT staged; only the three exec records this pass created (`P05.S23`, `P05.S24`, `P05.S25`) and the regenerated feature index were touched. The `P05.S25` checkbox is left UNFLIPPED for the team lead to close after this record lands. The four open rows (`P01.S05`, `P03.S16`, `P04.S20`, `P04.S21`) remain open by design pending their usage windows and are not counted as gaps - their exec records carry the exact one-command re-arm recipes.
