---
title: Universal Coding Standard
author: Vaultspec A2A
---
# Universal Coding Rules

1. All Python code MUST use type hints.
2. All tests MUST be integration tests (no mocks).
3. Use `uv` for all dependency management.
4. Prefer `httpx` over `requests`.
