# CLI Implementation Audit — 2026-03-06

## Source ADRs

- ADR-015: Dependency Hygiene, Pruning, OTel Mandate & CLI Entry Point
- ADR-016: Task Runner & Developer Bootstrap

## ADR-015 Compliance

| # | Requirement | Status | Evidence |
|---|------------|--------|----------|
| 1 | 11 dead deps removed | Implemented | `pyproject.toml:7-29` — none of the 11 dead deps present |
| 2 | OTel promoted to mandatory runtime | Implemented | `pyproject.toml:18-19` — 3 OTel packages in core deps |
| 3 | anyio as direct dep | Implemented | `pyproject.toml:8` — `anyio>=4.9.0` |
| 4 | `[project.scripts] vaultspec` | Implemented | `pyproject.toml:32` |
| 5 | deptry in dev deps | Implemented | `pyproject.toml:48` — `deptry>=0.23.0` in `[dependency-groups] dev` |
| 6 | `allow-direct-references` removed | Implemented | Not present in pyproject.toml |
| 7 | `rich>=14.3.2` in deps | Implemented | `pyproject.toml:22` |

## ADR-016 Compliance

| # | Requirement | Status | Evidence |
|---|------------|--------|----------|
| 1 | Justfile at repo root | Implemented | `Justfile` — 80 lines, 16 recipes |
| 2 | `set windows-shell` | Implemented | `Justfile:1` |
| 3 | `set dotenv-load` | Implemented | `Justfile:2` |
| 4 | `.env.example` | Implemented | `.env.example` — 91 lines, supersedes ADR spec |
| 5 | Node version pinning (volta) | Missing | No volta config in `src/ui/package.json`, no `.node-version` |

### Justfile Recipe Coverage

| ADR-016 Recipe | Status | Location |
|----------------|--------|----------|
| `setup` | Implemented | `Justfile:5` |
| `dev` | Implemented (divergent) | `Justfile:12` — includes worker auto-start |
| `test` | Implemented | `Justfile:44` |
| `lint` | Implemented | `Justfile:48` |
| `format` | Implemented | `Justfile:53` |
| `typecheck` | Implemented | `Justfile:58` |
| `audit` (deptry) | Missing | Not in Justfile |
| `build` | Missing | Not in Justfile |
| `clean` | Missing | Not in Justfile |
| `docker-build` | Implemented | `Justfile:67` |
| `docker-dev` | Renamed `up` | `Justfile:29` |

Extra recipes not in ADR-016: `dev-real`, `worker`, `up-prod`, `down`, `down-prod`, `check-ui`.

## Findings

| ID | Priority | Status | Description | File:Line | Fix Applied |
|----|----------|--------|-------------|-----------|-------------|
| CLI-001 | LOW | Open | `vaultspec-worker` entry point is undocumented — not in ADR-015 or ADR-016. Should be added to ADR-015 or a new ADR. | `pyproject.toml:33` | — |
| CLI-002 | LOW | Open | `Printer` class (Rich) is dead code — exported via `lib/utils/__init__.py:10` but zero consumers outside its own module and tests. | `lib/utils/printer.py`, `lib/utils/__init__.py:10` | — |
| CLI-003 | LOW | Open | Justfile missing 3 ADR-016 recipes: `audit` (deptry), `build`, `clean`. | `Justfile` | — |
| CLI-004 | INFO | Open | `rich>=14.3.2` is a core dependency. Only active runtime consumer is `RichHandler` in `logging.py:109`. `Printer` is dead code. Rich must stay in core deps because RichHandler is a runtime path for interactive terminals. | `pyproject.toml:22`, `lib/utils/logging.py:109` | — |
| CLI-005 | INFO | Open | No CLI argument parsing (no Typer/Click). Both `main()` functions use `uvicorn.run()` with pydantic-settings env vars. ADR-015 §6 Q1 deferred CLI args — current env-only approach is compliant. | `lib/api/app.py:323`, `lib/worker/app.py:140` | — |
| CLI-006 | LOW | Open | Node version pinning missing — ADR-016 §3.3 specifies volta config in `src/ui/package.json`. Not implemented. | `src/ui/package.json` | — |
| CLI-007 | INFO | Open | `lib/worker/__main__.py` exists (undocumented) — enables `python -m lib.worker` alongside the `vaultspec-worker` entry point. Not in any ADR. | `lib/worker/__main__.py` | — |

## Rich Dependency Usage

| File | Import | Consumer | Status |
|------|--------|----------|--------|
| `lib/utils/printer.py:8` | `from rich.console import Console` | `Printer` class (6 semantic methods) | Dead code — zero consumers |
| `lib/utils/logging.py:109` | `from rich.logging import RichHandler` | Interactive terminal log formatting | Live — conditional on `is_interactive` |
| `lib/utils/tests/test_printer.py:9` | `from rich.console import Console` | Test file | Test only |

---

## CLI Current vs Target Gap Analysis (Pass 1)

### Command mapping: current -> target

| Current CLI | Target CLI | Gap Status |
|-------------|-----------|------------|
| `vaultspec serve [--host] [--port] [--log-level]` | `vaultspec service start [backend]` | **RENAME + RESTRUCTURE** |
| `vaultspec worker [--port] [--log-level]` | `vaultspec service start [worker]` | **RENAME + RESTRUCTURE** |
| `vaultspec test [TARGET] [-- PYTEST_ARGS]` | `vaultspec test unit [PATH] [-- PYTEST_ARGS]` | **PARTIAL** -- no unit/smoke/benchmark subcommands |
| `vaultspec migrate upgrade [--target]` | `vaultspec database update [--target REVISION]` | **RENAME** |
| `vaultspec migrate stamp [--target]` | (removed) | **REMOVE** -- stamp has no target equivalent |
| `vaultspec config` | `vaultspec --show-config` | **RESTRUCTURE** -- subcommand to global flag |
| `vaultspec preps [SCENARIO]` | `vaultspec run mock [scenario]` | **RENAME** |
| `vaultspec eval smoke` | `vaultspec test benchmark smoke` | **RENAME** |
| `vaultspec eval nightly` | `vaultspec test benchmark nightly` | **RENAME** |
| (none) | `vaultspec run probe [provider]` | **NEW** |
| (none) | `vaultspec team start/status/resume/stop/delete/archive/list` | **NEW** -- entire group |
| (none) | `vaultspec agent ask/list` | **NEW** -- entire group |
| (none) | `vaultspec service stop/kill/delete` | **NEW** |
| (none) | `vaultspec database clear/snapshot/snapshot list/restore` | **NEW** |

### Detailed Findings

| ID | Severity | Status | Description | File:Line | Suggested Fix |
|----|----------|--------|-------------|-----------|---------------|
| CLI-010 | HIGH | Open | **`serve` and `worker` are top-level commands, target requires `service start` group.** `serve` at `cli.py:36-69` and `worker` at `cli.py:72-96` must become `service start [backend\|worker]`. Bare `service start` should launch both. | `src/vaultspec_a2a/cli.py:36-96` | Create `@cli.group() service`, add `start` subcommand with optional arg. |
| CLI-011 | HIGH | Open | **`test` command has no `unit`/`smoke`/`benchmark` subcommands.** Current `test` at `cli.py:99-121` takes free-form TARGET arg. Target requires `test` as a group with `unit`, `smoke`, `benchmark` subcommands. Bare `vaultspec test` defaults to `test unit`. | `src/vaultspec_a2a/cli.py:99-121` | Convert to group with `unit` (default), `smoke`, `benchmark` subcommands. |
| CLI-012 | HIGH | Open | **`config` is a subcommand, target requires `--show-config` global flag.** Current `config` at `cli.py:176-183` is a subcommand. Target: `--show-config` flag on root group with eager callback that prints and exits. | `src/vaultspec_a2a/cli.py:176-183` | Add `@click.option('--show-config', ...)` to root group. Delete `config` command. |
| CLI-013 | HIGH | Open | **`migrate` group uses wrong domain name.** Current `migrate` at `cli.py:127-170` should be `database`. `upgrade` -> `update`, `stamp` removed. New: `clear`, `snapshot`, `snapshot list`, `restore`. | `src/vaultspec_a2a/cli.py:127-170` | Rename group and subcommands. |
| CLI-014 | HIGH | Open | **`preps` command must become `run mock`.** Current `preps` at `cli.py:189-201` should move under a new `run` group as `mock` subcommand. | `src/vaultspec_a2a/cli.py:189-201` | Create `@cli.group() run`, add `mock` subcommand. |
| CLI-015 | HIGH | Open | **`eval` group must become `test benchmark`.** Current `eval` at `cli.py:207-231` with `smoke`/`nightly` should move under `test benchmark`. | `src/vaultspec_a2a/cli.py:207-231` | Remove `eval` group, add `benchmark` under `test` group. |
| CLI-016 | HIGH | Open | **`run probe` command missing.** Provider probes exist at `src/vaultspec_a2a/providers/probes/` with `run_probe` exported from `_protocol.py:25`. No CLI surface. | `src/vaultspec_a2a/providers/probes/_protocol.py` | Add `probe` subcommand under `run` group with provider choice arg. |
| CLI-017 | CRITICAL | Open | **`team` command group entirely missing.** Target requires 7 subcommands: `start`, `status`, `resume`, `stop`, `delete`, `archive`, `list`. Primary user-facing lifecycle surface. | (not implemented) | Create `team` group. Requires backend work (BE-001 through BE-004). |
| CLI-018 | CRITICAL | Open | **`agent` command group entirely missing.** Target requires: `ask` (single-agent execution) and `list` (glob agent presets). | (not implemented) | Create `agent` group. `ask` requires new backend path (BE-005). |
| CLI-019 | HIGH | Open | **`service stop/kill/delete` subcommands missing.** Only `start` (via current `serve`/`worker`) exists. | (not implemented) | Add to `service` group. Requires PID tracking (BE-009). |
| CLI-020 | HIGH | Open | **`database clear/snapshot/restore` subcommands missing.** Only `update` (via current `migrate upgrade`) exists. | (not implemented) | Add CRUD functions + CLI subcommands (BE-006 through BE-008). |
| CLI-021 | MED | Open | **`__all__` missing from `cli.py`.** Per ADR-009 facade pattern, all sub-modules must declare `__all__`. | `src/vaultspec_a2a/cli.py` | Add `__all__ = ["cli", "main"]` |
| CLI-022 | LOW | Open | **`main = cli` backward-compat alias at `cli.py:235`.** Target design has no mention of `main()`. If nothing imports it, remove. | `src/vaultspec_a2a/cli.py:235` | Verify no consumers, then delete. |

### Backend Gaps Blocking CLI

| ID | Severity | CLI Command | Backend Gap | Current State |
|----|----------|-------------|-------------|---------------|
| BE-001 | CRITICAL | `team delete` | No DELETE endpoint for threads. No `delete_thread()` CRUD function. | `endpoints.py` has POST/GET only. `crud.py` has no delete. |
| BE-002 | CRITICAL | `team archive` | No `ARCHIVED` in `ThreadStatus` enum. No archive endpoint. | `crud.py:36-44` enum: SUBMITTED, CREATED, RUNNING, COMPLETED, FAILED, CANCELLED. No ARCHIVED. |
| BE-003 | HIGH | `team list [status]` | `list_threads()` at `crud.py:178` has no status filter. `GET /threads` has no status query param. | Only pagination (offset, limit). |
| BE-004 | HIGH | `team resume --message` | No re-dispatch for completed/failed threads with new input. Resume at `endpoints.py:727` only handles permission interrupts. | Only works on interrupted threads. |
| BE-005 | HIGH | `agent ask` | No single-agent execution path. Needs: load agent TOML, wire provider + tools, single-node graph, stream response. | No code exists. |
| BE-006 | MED | `database clear` | No `truncate_tables()` CRUD function. | Not implemented. |
| BE-007 | MED | `database snapshot` | No SQLite file copy mechanism. | Not implemented. |
| BE-008 | MED | `database restore` | No snapshot restore. Needs running-service check. | Not implemented. |
| BE-009 | LOW | `service stop/kill` | No PID tracking or signal handling. No docker compose integration. | Not implemented. |

### Justfile Drift from Target CLI

Justfile recipes still use old command names. Must update after CLI restructure.

| Justfile Recipe | Current | Target |
|-----------------|---------|--------|
| `preps SCENARIO` (line 102) | `python -m vaultspec_a2a.tests.preps.{{SCENARIO}}` | `vaultspec run mock {{SCENARIO}}` |
| `preps-list` (line 106) | `python -m vaultspec_a2a.tests.preps` | `vaultspec run mock` |
| `eval-smoke` (line 110) | `python -m ...suites.smoke` | `vaultspec test benchmark smoke` |
| `eval-nightly` (line 114) | `python -m ...suites.nightly` | `vaultspec test benchmark nightly` |
| `worker` (line 19) | `uvicorn ...worker...` | `vaultspec service start worker` |
