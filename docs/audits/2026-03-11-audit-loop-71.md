# Audit Loop #71 — Pass 1 (2026-03-11)

## Scope

Fresh review pass after env-var audit sprint and reconciliation window fix.
Covers `src/vaultspec_a2a/` excluding test directories.

Checklist:
- [x] `os.environ` / `os.getenv` reads outside test code
- [x] Hardcoded operator-tunable constants
- [x] Silent `except Exception` without logging
- [x] Absolute imports of internal modules
- [x] Thread state transitions bypassing `update_thread_status`

---

## Findings

### FIXED this pass

| ID | Severity | File | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-01 | MEDIUM | `providers/acp_chat_model.py:1262,1281,1300,1319,1359` | Five management RPC calls use hardcoded `timeout=15` | Added `acp_rpc_timeout_seconds: float = 15.0` to Settings; wired at all 5 call sites |
| AUD-71-02 | MEDIUM | `providers/acp_chat_model.py:282` | `asyncio.Queue(maxsize=1024)` hardcoded | Added `acp_chunk_queue_maxsize: int = 1024` to Settings; wired at call site |
| AUD-71-03 | LOW | `api/app.py:267` | Silent `except Exception: checkpoint_unverified = True` swallows errors without logging | Added `logger.debug(..., exc_info=True)` |

### Accepted — not actionable

| ID | Severity | File | Finding | Why accepted |
|----|----------|------|---------|--------------|
| AUD-71-A1 | — | `telemetry/instrumentation.py:59-82` | Multiple `os.environ.get(OTEL_*)` at module scope | TEL-M5 accepted exception — OTel SDK vars must be read at import time before circular-import-safe Settings is available |
| AUD-71-A2 | — | `workspace/environment.py:111` | `os.environ.items()` to build subprocess env | Intentional — copies parent process environment to child; not a config read |
| AUD-71-A3 | — | `providers/probes/_protocol.py:297` | `os.environ.copy()` | Same — subprocess env construction |
| AUD-71-A4 | — | `database/migrations/env.py:23` | Absolute import `from vaultspec_a2a.database.models import ...` | Alembic env.py runs as a standalone script; absolute imports are required by the Alembic runtime |
| AUD-71-A5 | — | `utils/trace.py:105-106` | `except Exception: pass` in LangSmith diagnostic | Diagnostic-only utility; swallowing is intentional (non-critical trace fetch) |

### Deferred — low priority

| ID | Severity | File | Finding | Notes |
|----|----------|------|---------|-------|
| AUD-71-D1 | LOW | `cli/_util.py:68` | `timeout=30.0` for CLI HTTP client | CLI is a separate process; value is reasonable. Add `cli_http_timeout_seconds` to Settings if CLI timeout tuning becomes operationally necessary |
| AUD-71-D2 | LOW | `api/app.py:262` | `timeout=2.0` for aget_tuple in `_classify_missing_ws_thread` | Short-lived WS-rejection path; could be `settings.internal_aget_tuple_timeout_seconds` but risk/reward is low |

---

## Pass 2 Findings (2026-03-11 — operational robustness slice)

Fixes applied in this pass:

| ID | Severity | File | Finding | Resolution |
|----|----------|------|---------|------------|
| PROD-053 | HIGH | `docker-compose.{dev,prod,integration,postgres}.yml` | Gateway auto-spawns a second worker in Docker while compose already runs a dedicated worker service | Added `VAULTSPEC_AUTO_SPAWN_WORKER: 'false'` to gateway env in all four compose files |
| PROD-038 | MED | `worker/app.py` | Worker had no `/admin/shutdown` endpoint; CLI graceful shutdown fell back to SIGTERM immediately | Added `POST /admin/shutdown` → `os.kill(os.getpid(), signal.SIGTERM)` |
| WRK-K01 | LOW | `worker/health.py` | `HealthCheck` class was completely empty and imported nowhere | File confirmed absent (already deleted) |
| PROD-050 | HIGH | `api/endpoints.py` | DELETE `/threads/{id}` had no guard against RUNNING threads | Added pre-delete `get_thread` check; raises 409 if `status == RUNNING` |
| PROD-066 | HIGH | `api/endpoints.py`, `api/app.py` | `circuit_breaker.pre_dispatch()` blocked cancel when circuit was OPEN | Removed `pre_dispatch()` from both REST cancel and WS control cancel paths; CB still records success/failure from cancel dispatch results |
| PROD-067 | HIGH | `api/endpoints.py` | Worker 429 silently treated as success on thread creation; `record_success()` called, thread advanced to RUNNING but never executed | Capture response; on 429: mark thread FAILED, raise 503, no CB state change |
| PROD-068 | HIGH | `api/endpoints.py`, `api/app.py` | Worker 429 silently treated as success on `send_message` (REST) and WS dispatch | REST: raise 503 on 429. WS: raise `WebSocketCommandRejectedError(recoverable=True, code="WORKER_AT_CAPACITY")` |
| PLR0915 | LOW | `api/app.py` | `_create_dispatch_message_handler` exceeded ruff statement limit after 429 handling added | Extracted `_ws_mark_failed_and_broadcast` as a module-level helper; inlined 11-statement terminal-event block replaced with single call |

All `429` comparisons use `httpx.codes.TOO_MANY_REQUESTS` (no magic numbers).

---

## Coverage

- `os.environ` — clean (excluding accepted exceptions)
- Hardcoded constants — clean (remaining items deferred or accepted)
- Silent exception swallows — clean
- Import policy — clean (Alembic env.py absolute import accepted)
- Thread state machine — no bypasses found
- Worker 429 handling — clean
- Circuit breaker cancel bypass — clean
- Docker double-spawn — clean

Next pass: run after next implementation slice.

---

## Pass 3 Findings (2026-03-11 — observability architecture slice)

Scope:

- observability/debug-surface design trail for `#88`
- ACP runtime-authority design trail for `#89`
- verifier-evidence implications for `#87`

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-04 | HIGH | Observability architecture | Structured logs are not trace-correlated by default, so the current log stream cannot serve as an authoritative cross-service debug surface | Captured in new research + ADR-036. Follow-on implementation remains under `#88` |
| AUD-71-05 | HIGH | ACP/runtime authority | Local ACP and Docker-bundled provider runtime were both real authority surfaces, but the repo had no ADR stating where each is authoritative or how evidence should be classified | Captured in new research + ADR-037. Follow-on implementation remains under `#89` |
| AUD-71-06 | HIGH | Verifier evidence | `vaultspec test prodlike-docker` still lacks pre-teardown inspect/health evidence and a stable trace/log join model | Captured in new plan + audit trail. Implementation remains under `#87` |

Artifacts added this pass:

- `docs/research/2026-03-11-observability-debug-correlation-grounding.md`
- `docs/adrs/036-debug-evidence-surface.md`
- `docs/adrs/037-acp-runtime-authority.md`
- `docs/plans/2026-03-11-observability-architecture-plan.md`
- `docs/plans/2026-03-11-trace-debug-implementation-plan.md`

Review outcome:

- This pass was intentionally architecture-first, so no runtime code changed.
- The substantive deliverable was the missing authority model and execution
  plan, not a partial implementation without design closure.

Next pass:

- implement the first bounded `#88` slice against ADR-036
- review the actual diff
- then sync resulting findings back into this audit loop doc again

---

## Pass 4 Findings (2026-03-11 — trace/debug implementation planning slice)

Scope:

- turn the observability ADRs into an ordered implementation program
- decide whether a developer-facing trace/log review client belongs in scope
- decide whether forwarding general debug logs into Jaeger traces is an
  acceptable design

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-07 | HIGH | Implementation sequencing | The repo needed a concrete implementation order to avoid solving `#87` before `#88` / `#89` established the underlying evidence contract | Added `docs/plans/2026-03-11-trace-debug-implementation-plan.md` with phased order and success criteria |
| AUD-71-08 | HIGH | Debug evidence model | Forwarding general debug logs into Jaeger traces would collapse distinct telemetry signals and make Jaeger do the wrong job | Rejected as the default design in the implementation plan; logs remain authoritative debug evidence |
| AUD-71-09 | MEDIUM | Developer tooling | A developer-facing review client is acceptable only if it remains downstream of authoritative logs, traces, and verifier artifacts | Captured as optional Phase 5 in the implementation plan rather than part of the authority model |

Artifacts added this pass:

- `docs/plans/2026-03-11-trace-debug-implementation-plan.md`

Review outcome:

- No new contradiction surfaced in the implementation plan.
- The first bounded implementation slice is now explicit:
  add automatic `trace_id` / `span_id` correlation in `utils/logging.py`, then
  prove it with focused tests before widening scope.

---

## Pass 5 Findings (2026-03-11 — `#88` Phase 1 implementation slice)

Scope:

- first bounded implementation slice for the observability track
- automatic trace/log correlation in the shared logging layer
- focused verification and review of the actual diff

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-10 | HIGH | `#88` implementation | Structured logs previously lacked automatic OTel correlation, leaving trace/log joins dependent on ad hoc caller extras | Fixed in `src/vaultspec_a2a/utils/logging.py` by adding an `OTelCorrelationFilter` that injects `trace_id`, `span_id`, `trace_sampled`, and `service_name` when an active span exists and the fields are absent |
| AUD-71-11 | HIGH | `#88` verification | Existing logging tests were too thin to prove active-span injection, no-active-span behavior, collision preservation, and handler-path attachment | Fixed in `src/vaultspec_a2a/utils/tests/test_logging.py` with focused coverage for active real span correlation, no-active-span emission, caller field preservation, JSON output integration, and filter attachment on JSON and Rich paths |
| AUD-71-12 | MEDIUM | follow-on scope | The Phase 1 slice improves the log substrate only; high-value runtime keys such as `thread_id`, `dispatch_id`, and `client_id` remain absent from many important call sites | Left open intentionally for the next follow-on `#88` slice; the priority order is gateway dispatch paths, WebSocket command/rejection paths, then worker IPC relay paths |

Implementation evidence:

- `src/vaultspec_a2a/utils/logging.py`
- `src/vaultspec_a2a/utils/tests/test_logging.py`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/utils/logging.py src/vaultspec_a2a/utils/tests/test_logging.py`
- `python -m pytest src/vaultspec_a2a/utils/tests/test_logging.py -q`
- reported result: `8 passed`

Review outcome:

- No new product defect surfaced in the Phase 1 slice itself.
- One residual operational note remains outside the slice boundary: this does
  not yet enrich service-path logs with runtime-owned identifiers beyond the
  newly automatic OTel correlation fields.

Next pass:

- dispatch the follow-on `#88` slice for structured `extra={...}` enrichment in
  `api/endpoints.py`, `api/websocket.py`, and `worker/ipc.py`
- then re-run review and queue sync before moving to `#89`

---

## Pass 6 Findings (2026-03-11 — `#88` Phase 2 service-path enrichment slice)

Scope:

- high-value runtime-field enrichment at top-priority service-path call sites
- bounded to gateway endpoint dispatch paths, WebSocket command/rejection paths,
  and worker IPC relay paths
- focused verification and review of the actual diff

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-13 | HIGH | `#88` service-path evidence | Gateway, WebSocket, and worker IPC logs still emitted without runtime-owned identifiers at the main dispatch and rejection boundaries, leaving the new OTel substrate underused for real debugging | Fixed in `src/vaultspec_a2a/api/endpoints.py`, `src/vaultspec_a2a/api/websocket.py`, and `src/vaultspec_a2a/worker/ipc.py` by attaching bounded `extra={...}` fields where ids were already naturally in hand, including `thread_id`, `dispatch_id`, `request_id`, `client_id`, `agent_id`, `worker_id`, and action/error metadata |
| AUD-71-14 | MEDIUM | `#88` verification | The existing endpoint/WebSocket/IPC tests proved behavior but not that the structured runtime fields were actually present on emitted log records | Fixed in `src/vaultspec_a2a/api/tests/test_endpoints.py`, `src/vaultspec_a2a/api/tests/test_websocket.py`, and `src/vaultspec_a2a/worker/tests/test_ipc.py` with focused `caplog` assertions on emitted runtime fields |
| AUD-71-15 | MEDIUM | residual scope | The current enrichment covers only the top-priority service paths; other operationally relevant paths still lack explicit runtime fields | Left open intentionally for later `#88` follow-on work outside this bounded slice; next candidates remain secondary gateway/service paths and verifier artifact consumers |

Implementation evidence:

- `src/vaultspec_a2a/api/endpoints.py`
- `src/vaultspec_a2a/api/websocket.py`
- `src/vaultspec_a2a/worker/ipc.py`
- `src/vaultspec_a2a/api/tests/test_endpoints.py`
- `src/vaultspec_a2a/api/tests/test_websocket.py`
- `src/vaultspec_a2a/worker/tests/test_ipc.py`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/api/endpoints.py src/vaultspec_a2a/api/websocket.py src/vaultspec_a2a/worker/ipc.py src/vaultspec_a2a/api/tests/test_websocket.py src/vaultspec_a2a/api/tests/test_endpoints.py src/vaultspec_a2a/worker/tests/test_ipc.py`
- `python -m pytest src/vaultspec_a2a/api/tests/test_websocket.py src/vaultspec_a2a/api/tests/test_endpoints.py src/vaultspec_a2a/worker/tests/test_ipc.py -q`
- reported result: `65 passed`, with one non-failing `PytestCacheWarning` due to `.pytest_cache` write denial in the workspace

Review outcome:

- No new product defect surfaced inside the bounded Phase 2 slice itself.
- One remaining limitation is now explicit: `#88` still needs broader coverage
  beyond these top-priority call sites before the debug surface is complete.

Next pass:

- continue `#88` only where additional high-signal runtime fields are already
  naturally available and verifier consumption benefits directly
- keep `#89` and `#87` blocked on the now-improved evidence contract rather
  than bypassing it

---

## Pass 7 Findings (2026-03-11 — `#88` internal boundary enrichment slice)

Scope:

- bounded follow-on enrichment in `src/vaultspec_a2a/api/internal.py`
- merge-safe against unrelated in-flight execution-state work already present in
  `internal.py` and `test_internal.py`
- focused verification and review of the actual diff

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-16 | HIGH | `#88` internal relay evidence | The internal worker-to-gateway relay boundary still lacked structured runtime fields on terminal-status, malformed-envelope, relay-drop, and heartbeat logs, leaving a major cross-boundary evidence seam weak even after Phase 2 | Fixed in `src/vaultspec_a2a/api/internal.py` by attaching bounded `extra={...}` fields to the existing terminal-status, malformed envelope, connection-manager-unavailable, internal WS heartbeat, unknown-message-type, and HTTP heartbeat log statements |
| AUD-71-17 | MEDIUM | `#88` verification | `api/tests/test_internal.py` covered internal relay behavior but did not prove that structured runtime fields were present on emitted records at the internal boundary | Fixed in `src/vaultspec_a2a/api/tests/test_internal.py` with focused `caplog` assertions for terminal update/skip logs, malformed WS envelope warnings, relay-drop warnings, internal WS heartbeat/unknown-type logs, and HTTP heartbeat logs |
| AUD-71-18 | MEDIUM | residual scope | `#88` now covers the shared logger, top-priority service paths, and the internal relay boundary, but worker executor paths and verifier evidence consumers still remain outside the implemented surface | Left open intentionally for the next `#88` follow-on slice; priority remains `src/vaultspec_a2a/worker/executor.py`, then `src/vaultspec_a2a/cli/_verify.py` |

Implementation evidence:

- `src/vaultspec_a2a/api/internal.py`
- `src/vaultspec_a2a/api/tests/test_internal.py`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/api/internal.py src/vaultspec_a2a/api/tests/test_internal.py`
- `python -m pytest src/vaultspec_a2a/api/tests/test_internal.py -q`
- reported result: `26 passed`, with one non-failing `PytestCacheWarning` due to `.pytest_cache` write denial in the workspace

Review outcome:

- No new product defect surfaced inside the bounded internal-boundary slice.
- One test assumption failed during the first verification pass because the
  fixture thread state was not in a valid pre-terminal state; the test was
  corrected to establish `RUNNING` through the real CRUD path before asserting
  the terminal-update log. No runtime code defect was involved.

---

## Pass 8 Findings (2026-03-11 — `#88` executor-path enrichment slice)

Scope:

- bounded follow-on enrichment in `src/vaultspec_a2a/worker/executor.py`
- merge-safe against unrelated in-flight executor changes already present in
  `executor.py` and `test_executor.py`
- focused verification and review of the actual diff

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-19 | HIGH | `#88` executor-path evidence | Worker executor logs still emitted at key dispatch, pre-flight, compile, rejection, and failure seams without the runtime-owned identifiers needed to correlate worker-side failures back to gateway dispatches and worker instances | Fixed in `src/vaultspec_a2a/worker/executor.py` by attaching bounded `extra={...}` fields to existing executor log statements, including `thread_id`, `dispatch_id`, `dispatch_action`, `worker_id`, `agent_id`, `team_preset`, `runtime_mode`, `active_thread_count`, action labels, and bounded outcome/error metadata |
| AUD-71-20 | MEDIUM | `#88` verification | `worker/tests/test_executor.py` covered executor control flow but did not prove that structured runtime fields were present on emitted executor log records | Fixed in `src/vaultspec_a2a/worker/tests/test_executor.py` with focused `caplog` assertions for ingest graph-missing warnings, resume graph-missing warnings, and concurrent-ingest rejection warnings |
| AUD-71-21 | MEDIUM | residual scope | `#88` now covers the shared logger, top-priority service paths, internal relay boundary, and worker executor seams, but verifier artifact capture and evidence-manifest consumption still remain outside the implemented surface | Left open intentionally for the next follow-on slice in `src/vaultspec_a2a/cli/_verify.py` under the existing `#87` verifier diagnostics work |

Implementation evidence:

- `src/vaultspec_a2a/worker/executor.py`
- `src/vaultspec_a2a/worker/tests/test_executor.py`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/worker/executor.py src/vaultspec_a2a/worker/tests/test_executor.py`
- `python -m pytest src/vaultspec_a2a/worker/tests/test_executor.py -q`
- reported result: `25 passed`, with one non-failing `PytestCacheWarning` due to `.pytest_cache` write denial in the workspace and a non-failing Ruff cache write denial warning

Review outcome:

- No new product defect surfaced inside the bounded executor slice.
- The executor-path enrichment stayed within existing log sites and did not
  require control-flow changes beyond the already in-flight executor work.
- The meaningful remaining observability gap is now concentrated in verifier
  evidence capture and correlation-aware artifact consumption rather than the
  primary runtime execution surfaces.

---

## Pass 9 Findings (2026-03-11 — `#87` verifier evidence slice)

Scope:

- bounded verifier hardening in `src/vaultspec_a2a/cli/_verify.py`
- focused on pre-teardown diagnostics, correlation-aware artifact manifests,
  and provider probe artifact persistence
- focused verification and review of the actual diff

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-22 | HIGH | `#87` verifier evidence | `vaultspec test prodlike-docker` and `vaultspec test prodlike-provider` still produced too-thin pre-teardown evidence, especially around readiness timeouts, effective compose state, container inspect data, and provider subprocess output | Fixed in `src/vaultspec_a2a/cli/_verify.py` by recording readiness probe history, effective compose config, per-service container inspect artifacts, correlation-aware `evidence-manifest.json`, richer trace manifests with `trace_id` lists, and provider probe stdout/stderr artifacts before teardown |
| AUD-71-23 | MEDIUM | `#87` verification | The CLI verifier had no focused tests proving the new evidence helpers actually persisted readiness history, provider probe artifacts, and evidence-manifest correlations | Fixed in `src/vaultspec_a2a/cli/tests/test_verify.py` with focused coverage for readiness-probe history over a real local HTTP path, provider stdout/stderr artifact persistence, and evidence-manifest correlation fields |
| AUD-71-24 | MEDIUM | residual scope | The verifier now preserves a materially better diagnostic bundle, but the slice has not yet re-run the full elevated prod-like Docker verifier to prove the new artifacts classify the original gateway-readiness timeout end to end | Left open intentionally as the next follow-on under `#87`; the code path is hardened, but a fresh live verifier run still needs to consume the new artifacts against a real failure or success path |

Implementation evidence:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/cli/tests/test_verify.py`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/cli/_verify.py src/vaultspec_a2a/cli/tests/test_verify.py`
- `python -m pytest src/vaultspec_a2a/cli/tests/test_verify.py -q`
- reported result: `3 passed`, with one non-failing `PytestCacheWarning` due to `.pytest_cache` write denial in the workspace and a non-failing Ruff cache write denial warning

Review outcome:

- No new product defect surfaced inside the bounded verifier slice.
- The only surfaced issue during implementation was test-environment drift:
  pytest temp-root writes were denied in this Windows workspace, so the focused
  tests were moved onto an explicit writable artifact root instead of relying on
  default temp-dir fixtures.

---

## Pass 10 Findings (2026-03-11 — `#87` gateway runtime-path fix + live rerun)

topic:

- `#87` prodlike-docker rerun after fixing the gateway startup permission crash

audit surface:

- gateway startup/runtime-path handling in `src/vaultspec_a2a/api/app.py`
- live `uv run vaultspec test prodlike-docker` recertification and artifact
  classification

rewrite scope:

- audit-loop entry plus the matching `#87` / `#88` status updates in the
  continuous and consolidated audits

Scope:

- bounded startup-path fix in `src/vaultspec_a2a/api/app.py`
- focused gateway app verification in `src/vaultspec_a2a/api/tests/test_app.py`
- one fresh elevated prod-like Docker rerun against the real compose stack

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-25 | HIGH | `#87` gateway startup | The gateway eagerly resolved the auto-spawn worker stderr log path during `LazyWorkerSpawner` construction, so prod-like Docker startup tried to create `/app/.vaultspec/runtime` as `appuser` and crashed even with `VAULTSPEC_AUTO_SPAWN_WORKER=false` | Fixed in `src/vaultspec_a2a/api/app.py` by making the gateway-managed stderr log path conditional on `auto_spawn` and preserving `None` on the watchdog/app-state handoff when auto-spawn is disabled |
| AUD-71-26 | MEDIUM | `#87` verification | The gateway app tests did not prove the no-autospawn path stayed free of startup-owned runtime-log path requirements | Fixed in `src/vaultspec_a2a/api/tests/test_app.py` with focused coverage proving `LazyWorkerSpawner(auto_spawn=False)` leaves `stderr_log_path` unset and `WorkerWatchdog` preserves that null diagnostic state |
| AUD-71-27 | HIGH | live recertification follow-on | The fresh elevated prod-like rerun proved the gateway startup crash is fixed, but surfaced a new certifying failure: Jaeger trace verification still returns zero traces for both `vaultspec-a2a` and `vaultspec-worker` even though gateway, worker, Postgres, and Jaeger all reach healthy state | Left open under `#87` as runtime trace/export triage. The failure is preserved in `.vaultspec/runtime/verify-prodlike-docker/20260311T152303Z` with healthy compose state, request-serving gateway logs, and `trace-manifest.json` showing zero traces for both services |

Implementation evidence:

- `src/vaultspec_a2a/api/app.py`
- `src/vaultspec_a2a/api/tests/test_app.py`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T150927Z`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T152303Z`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/api/app.py src/vaultspec_a2a/api/tests/test_app.py`
- `python -m pytest src/vaultspec_a2a/api/tests/test_app.py -q`
- `uv run vaultspec test prodlike-docker`
- reported results:
  - `ruff`: passed, with a non-failing Ruff cache write denial warning
  - targeted `pytest`: `10 passed`, with one non-failing `PytestCacheWarning`
  - elevated prod-like rerun: the original gateway readiness failure is fixed,
    but the verifier now fails at `Jaeger trace verification failed; no traces found for services: vaultspec-a2a, vaultspec-worker`

Review outcome:

- No new defect surfaced inside the bounded `app.py` fix itself.
- The live rerun proves the runtime-path regression was real and is now fixed:
  the gateway no longer restarts on `/app/.vaultspec` permission errors.
- `#87` remains open, but it is no longer a gateway-startup or thin-diagnostics
  issue. The remaining certifying gap is trace-export / Jaeger-query triage on
  an otherwise healthy prod-like stack.

---

## Pass 11 Findings (2026-03-11 — `#87` verifier Jaeger query-contract fix + successful prod-like recertification)

topic:

- `#87` verifier Jaeger query semantics and healthy-stack recertification

audit surface:

- prod-like Docker verifier trace-query semantics in `src/vaultspec_a2a/cli/_verify.py`
- live `uv run vaultspec test prodlike-docker` recertification against the real
  compose stack

rewrite scope:

- audit-loop entry plus the matching `#87` / `#88` status updates in the
  continuous and consolidated audits

Scope:

- bounded verifier fix in `src/vaultspec_a2a/cli/_verify.py`
- focused verifier tests in `src/vaultspec_a2a/cli/tests/test_verify.py`
- one fresh elevated prod-like Docker rerun against the real compose stack

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-28 | HIGH | `#87` verifier trace contract | The prod-like verifier queried Jaeger with `start` / `end` windows and required traces for `vaultspec-worker` even though the certifying flow only exercised gateway thread creation/state reads, causing a healthy stack to fail trace verification for the wrong reasons | Fixed in `src/vaultspec_a2a/cli/_verify.py` by switching Jaeger querying to the repo's grounded `service + lookback` pattern, scoping trace diagnostics/manifests to the queried service set, and requiring only `vaultspec-a2a` traces for `verify_prodlike_docker()` |
| AUD-71-29 | HIGH | `#87` live recertification | `#87` remained open until a real elevated prod-like rerun proved the verifier-side Jaeger query/contract fix worked end to end against the live compose stack | Fixed and verified by `uv run vaultspec test prodlike-docker`, which passed on 2026-03-11 and preserved the success bundle at `.vaultspec/runtime/verify-prodlike-docker/20260311T154053Z` with `trace-manifest.json` showing `vaultspec-a2a` `trace_count: 4` and `evidence-manifest.json` recording `thread_id`, readiness probes, and scoped trace services |

Implementation evidence:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/cli/tests/test_verify.py`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T152303Z`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T154053Z`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/cli/_verify.py src/vaultspec_a2a/cli/tests/test_verify.py`
- `python -m pytest src/vaultspec_a2a/cli/tests/test_verify.py -q`
- `uv run vaultspec test prodlike-docker`
- reported results:
  - `ruff`: passed, with a non-failing Ruff cache write denial warning
  - targeted `pytest`: `5 passed`, with one non-failing `PytestCacheWarning`
  - elevated prod-like rerun: passed, with certification evidence preserved in
    `.vaultspec/runtime/verify-prodlike-docker/20260311T154053Z`

Review outcome:

- No new product defect surfaced inside the bounded verifier fix itself.
- `#87` is now fixed and verified for the baseline prod-like Docker flow.
- `#88` remains complete on runtime emission and verifier-consumption surfaces.
- `#89` remains separate and unchanged.

---

## Pass 12 Findings (2026-03-11 — `#89` Phase 1 ACP runtime-authority + subprocess evidence slice)

topic:

- `#89` Phase 1 worker-owned ACP runtime-authority classification and bounded
  subprocess lifecycle evidence

audit surface:

- provider runtime-authority resolution in `src/vaultspec_a2a/providers/factory.py`
- ACP subprocess spawn/termination evidence in
  `src/vaultspec_a2a/providers/_subprocess.py`
- bounded handshake failure context in
  `src/vaultspec_a2a/providers/acp_chat_model.py`

rewrite scope:

- audit-loop entry plus the matching `#89` status updates in the continuous and
  consolidated audits

Scope:

- bounded `#89` Phase 1 implementation in
  `src/vaultspec_a2a/providers/factory.py`
- bounded subprocess lifecycle evidence in
  `src/vaultspec_a2a/providers/_subprocess.py`
- narrow ACP handshake/error evidence in
  `src/vaultspec_a2a/providers/acp_chat_model.py`
- focused provider tests only

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-30 | HIGH | `#89` runtime authority | ACP provider runs still emitted too little structure to distinguish local-native, Docker-bundled, project-local, and system-CLI runtime authority at the worker-owned boundary, so failures collapsed into generic provider startup noise | Fixed in `src/vaultspec_a2a/providers/factory.py` by classifying resolved Claude/Gemini command paths into bounded `runtime_authority`, `command_origin`, `command_kind`, `command_executable`, `command_target`, `acp_backend`, and `auth_mode` metadata and passing that evidence into `AcpChatModel` |
| AUD-71-31 | HIGH | `#89` subprocess/handshake evidence | ACP subprocess lifecycle and early handshake failures still lacked bounded evidence for spawn start/failure/success, pid, cwd, spawn mode, termination escalation, initialize/session step, and early subprocess exit | Fixed in `src/vaultspec_a2a/providers/_subprocess.py` and `src/vaultspec_a2a/providers/acp_chat_model.py` by adding structured spawn/termination logs, bounded stderr event counting, handshake-step context for `initialize` / `session/new` / `session/load` failures, and explicit early-exit logs without widening ACP protocol behavior |
| AUD-71-32 | MEDIUM | `#89` verification + residual scope | The Phase 1 slice proves the bounded metadata contract in focused provider tests, but it does not yet extend the same evidence model into manual probe/certifying Docker-provider runs, and real asyncio subprocess-pipe lifecycle coverage remains environment-limited in this Windows workspace | Left open intentionally for Phase 2 / `#86`; focused coverage landed in `src/vaultspec_a2a/providers/tests/test_factory.py`, `src/vaultspec_a2a/providers/tests/test_acp_chat_model.py`, and `src/vaultspec_a2a/providers/tests/test_subprocess.py`, while live Docker credential-backed provider certification remains separate |

Implementation evidence:

- `src/vaultspec_a2a/providers/factory.py`
- `src/vaultspec_a2a/providers/_subprocess.py`
- `src/vaultspec_a2a/providers/acp_chat_model.py`
- `src/vaultspec_a2a/providers/tests/test_factory.py`
- `src/vaultspec_a2a/providers/tests/test_acp_chat_model.py`
- `src/vaultspec_a2a/providers/tests/test_subprocess.py`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/providers/factory.py src/vaultspec_a2a/providers/_subprocess.py src/vaultspec_a2a/providers/acp_chat_model.py src/vaultspec_a2a/providers/tests/test_factory.py src/vaultspec_a2a/providers/tests/test_acp_chat_model.py src/vaultspec_a2a/providers/tests/test_subprocess.py`
- `python -m pytest src/vaultspec_a2a/providers/tests/test_factory.py src/vaultspec_a2a/providers/tests/test_acp_chat_model.py src/vaultspec_a2a/providers/tests/test_subprocess.py -q`
- reported results:
  - `ruff`: passed, with a non-failing Ruff cache write denial warning
  - targeted `pytest`: `31 passed, 8 deselected`, with one non-failing
    `PytestCacheWarning`

Review outcome:

- No new product defect surfaced inside the bounded Phase 1 slice itself.
- One verification constraint remains explicit rather than hidden: real
  asyncio subprocess-pipe lifecycle coverage hit `PermissionError:
  [WinError 5] Access is denied` in this Windows workspace, so the landed test
  surface stays focused on bounded metadata contracts and existing provider
  behavior rather than a brittle fake or platform-specific skip.
- `#89` should no longer be described as implementation-pending.
  The current state is now:
  - architecture grounded
  - Phase 1 runtime-authority and subprocess evidence implemented and verified
  - follow-on runtime-boundary completion still pending under `#89`

---

## Pass 13 Findings (2026-03-11 — `#86` live Claude Docker provider certification classification)

topic:

- `#86` live Claude Docker provider certification after `#89` Phase 1 evidence
  landing

audit surface:

- Dockerized Claude provider certification via
  `uv run vaultspec test prodlike-provider claude`
- worker-owned ACP runtime-authority and subprocess lifecycle evidence
- provider quota/account-state classification versus generic Docker/provider
  failure

rewrite scope:

- audit-loop entry plus the matching `#86` status updates in the continuous and
  consolidated audits

Scope:

- live Docker-backed Claude provider certification only
- failure bundle at
  `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z`
- no production code changes in this pass

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-33 | HIGH | `#86` Claude Docker certification | The live Docker-backed Claude provider certification no longer fails as generic provider/runtime noise: the stack was healthy, the ACP subprocess spawned, `initialize` and `session/new` both succeeded, and the failure narrowed to `session/prompt` with provider error `Internal error: You've hit your limit · resets Mar 13, 4am (UTC)` | Classified as a provider quota/account-state failure under `#86`, not packaging, spawn, generic auth absence, or Docker runtime-authority breakage; evidence is preserved in `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/claude.probe.stdout.txt`, `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/claude.probe.json`, and `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/evidence-manifest.json` |
| AUD-71-34 | MEDIUM | `#86` Gemini Docker certification | The current Docker provider certification surface still depends on `GEMINI_API_KEY` or `GOOGLE_API_KEY`, and the current `.env` does not provide a non-empty Gemini env-key for that path | Left open under `#86`; Gemini remains unverified in this environment until a non-empty env-key is supplied for the Docker cert surface |

Implementation evidence:

- `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/claude.probe.stdout.txt`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/claude.probe.json`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/evidence-manifest.json`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T205100Z/trace-manifest.json`

Verification completed:

- `uv run vaultspec test prodlike-provider claude`
- reported result:
  - stack healthy
  - ACP `initialize` and `session/new` succeeded
  - `session/prompt` failed with provider quota/account-state error

Review outcome:

- `#86` remains open, but the Claude Docker path is now classifiable as a
  provider quota/account-state issue rather than a generic Docker/provider
  runtime failure.
- `#89` Phase 1 evidence did its intended job here: subprocess launch,
  handshake phase, and failure boundary are explicit.
- Immediate next operational step:
  - Claude: retry after quota reset or with a different credential/account that
    has available capacity
  - Gemini: provide a non-empty `GEMINI_API_KEY` or `GOOGLE_API_KEY` for the
    Docker cert surface before attempting certification
- Carry `#86` forward for explicit pickup on Friday, March 13, 2026: rerun the
  Claude Docker certification after the provider reset at 4:00 AM UTC.

---

## Pass 14 Findings (2026-03-11 — `#86` Gemini Docker OAuth-backed certification path)

topic:

- `#86` Gemini Docker provider certification using mounted local OAuth-backed
  Gemini CLI state instead of env-key auth

audit surface:

- `uv run vaultspec test prodlike-provider gemini`
- Docker worker provider overlay and Gemini CLI auth-material path
- Gemini ACP `initialize` / `session/new` boundary under mounted CLI state

rewrite scope:

- audit-loop entry plus the matching `#86` status updates in the continuous and
  consolidated audits, plus the Docker auth-path note

Scope:

- production code changes in the Gemini Docker auth-material path
- focused provider/verifier tests
- live Docker-backed Gemini certification attempts at:
  - `.vaultspec/runtime/verify-prodlike-docker/20260311T215323Z`
  - `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z`

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-35 | HIGH | `#86` Gemini Docker auth surface | The Docker Gemini cert surface was previously hard-gated to `GEMINI_API_KEY` / `GOOGLE_API_KEY`, which did not match the real host-authenticated Gemini CLI surface available in this environment | Fixed in `src/vaultspec_a2a/cli/_verify.py`, `src/vaultspec_a2a/providers/gemini_auth.py`, `src/vaultspec_a2a/providers/factory.py`, `src/vaultspec_a2a/providers/probes/gemini.py`, and `docker-compose.prod.providers.yml` by adding mounted `GEMINI_CLI_HOME` support, preserving the OAuth-backed Gemini CLI state root, and passing explicit Gemini auth-selector env into the worker-owned ACP subprocess path |
| AUD-71-36 | HIGH | `#86` Gemini Docker certification | A real Docker-backed Gemini certification now reaches a narrower failure boundary: the worker sees the mounted Gemini CLI state, the Python refresh helper can read and refresh the OAuth token successfully, ACP `initialize` succeeds, but Gemini CLI still rejects `session/new` with `Authentication required` | Left open under `#86`; the residual blocker is now the Gemini CLI ACP auth/session contract rather than missing Docker auth material, generic startup failure, or missing trace/debug evidence. Evidence is preserved in `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z/gemini.probe.stdout.txt`, `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z/gemini.probe.json`, and `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z/evidence-manifest.json` |

Implementation evidence:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/providers/gemini_auth.py`
- `src/vaultspec_a2a/providers/factory.py`
- `src/vaultspec_a2a/providers/probes/gemini.py`
- `docker-compose.prod.providers.yml`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z/gemini.probe.stdout.txt`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z/gemini.probe.json`
- `.vaultspec/runtime/verify-prodlike-docker/20260311T220148Z/evidence-manifest.json`

Verification completed:

- `ruff check src/vaultspec_a2a/core/config.py src/vaultspec_a2a/providers/gemini_auth.py src/vaultspec_a2a/providers/factory.py src/vaultspec_a2a/providers/probes/gemini.py src/vaultspec_a2a/cli/_verify.py src/vaultspec_a2a/providers/tests/test_gemini_auth.py src/vaultspec_a2a/providers/tests/test_factory.py src/vaultspec_a2a/cli/tests/test_verify.py`
- `pytest src/vaultspec_a2a/providers/tests/test_gemini_auth.py src/vaultspec_a2a/providers/tests/test_factory.py src/vaultspec_a2a/cli/tests/test_verify.py -q`
- `uv run vaultspec test prodlike-provider gemini`
- reported result:
  - mounted local Gemini CLI state is visible in the worker-owned verification path
  - OAuth token refresh succeeds inside the container-backed probe
  - ACP `initialize` succeeds
  - ACP `session/new` still fails with `Authentication required`

Review outcome:

- `#86` remains open, but the Gemini Docker path is no longer blocked by
  missing env-key auth alone.
- The new supported Docker auth-material surface is now explicit:
  local OAuth-backed Gemini CLI state via `GEMINI_CLI_HOME` mount plus the
  worker-owned probe path.
- The remaining bug is narrower and likely inside the Gemini CLI ACP
  auth/session contract for non-interactive `session/new`, not in Docker auth
  material discovery or token refresh.

---

## Pass 15 Findings (2026-03-11 — runtime path policy + Gemini Docker auth alignment)

topic:

- mutable runtime-artifact path policy and final Gemini Docker OAuth-backed
  provider certification

audit surface:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/cli/_service.py`
- `src/vaultspec_a2a/api/app.py`
- `src/vaultspec_a2a/providers/factory.py`
- `docker-compose.prod.providers.yml`
- `uv run vaultspec test prodlike-provider gemini`

rewrite scope:

- audit-loop entry plus the matching current-status updates in the continuous
  and consolidated audits, and the Docker runtime-path note

Scope:

- move mutable runtime outputs out of `.vaultspec`
- align Gemini subprocess `HOME` with mounted `GEMINI_CLI_HOME`
- certify the live Docker Gemini provider path again after the auth fix

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-37 | HIGH | runtime path policy | Mutable verifier bundles, CLI-managed service state/logs, and gateway-managed worker stderr logs were still emitted under `.vaultspec/runtime`, which conflicts with the protected role of `.vaultspec` in this repo | Fixed in `src/vaultspec_a2a/cli/_verify.py`, `src/vaultspec_a2a/cli/_service.py`, and `src/vaultspec_a2a/api/app.py` by moving mutable runtime outputs to `.vault/runtime`; `src/vaultspec_a2a/tests/test_repo_hygiene.py` now blocks `.vaultspec/runtime` from reappearing in those runtime writers, and `src/vaultspec_a2a/api/tests/test_app.py` now asserts the worker stderr log path is under `.vault/runtime` |
| AUD-71-38 | HIGH | `#86` Gemini Docker certification | The worker container had mounted Gemini OAuth state at `GEMINI_CLI_HOME`, but the Gemini CLI subprocess still resolved user settings from `HOME=/home/appuser`, so it lost `security.auth.selectedType=oauth-personal` and fell back to unauthenticated `session/new` | Fixed in `src/vaultspec_a2a/providers/factory.py` and `docker-compose.prod.providers.yml` by aligning subprocess/container `HOME` with `GEMINI_CLI_HOME`; a fresh real `uv run vaultspec test prodlike-provider gemini` rerun passed on March 11, 2026 and preserved the success bundle at `.vault/runtime/verify-prodlike-docker/20260311T230801Z` |
| AUD-71-39 | MEDIUM | containerized Gemini workspace settings | The now-passing Docker Gemini cert run still emits non-fatal Gemini CLI stderr noise from project-level MCP extension discovery inside the worker image, including `spawn npx ENOENT` for `context7` and connection-closed warnings for `nanobanana` | Left open as container UX/noise reduction follow-up; the provider cert still passed end to end, but the worker image does not currently contain every MCP runtime referenced by the mounted workspace `.gemini/settings.json` |

Implementation evidence:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/cli/_service.py`
- `src/vaultspec_a2a/api/app.py`
- `src/vaultspec_a2a/providers/factory.py`
- `docker-compose.prod.providers.yml`
- `.vault/runtime/verify-prodlike-docker/20260311T230801Z/gemini.probe.stdout.txt`
- `.vault/runtime/verify-prodlike-docker/20260311T230801Z/gemini.probe.json`
- `.vault/runtime/verify-prodlike-docker/20260311T230801Z/evidence-manifest.json`

Verification completed:

- `python -m ruff check src/vaultspec_a2a/cli/_verify.py src/vaultspec_a2a/cli/_service.py src/vaultspec_a2a/api/app.py src/vaultspec_a2a/api/tests/test_app.py src/vaultspec_a2a/tests/test_repo_hygiene.py src/vaultspec_a2a/providers/factory.py src/vaultspec_a2a/providers/tests/test_factory.py`
- `pytest src/vaultspec_a2a/api/tests/test_app.py src/vaultspec_a2a/cli/tests/test_verify.py src/vaultspec_a2a/cli/tests/test_service.py src/vaultspec_a2a/tests/test_repo_hygiene.py src/vaultspec_a2a/providers/tests/test_factory.py -q`
- `uv run vaultspec test prodlike-provider gemini`
- reported result:
  - mutable runtime outputs now emit under `.vault/runtime`
  - mounted Gemini OAuth-backed state remains visible in the worker-owned path
  - Gemini ACP `initialize`, `session/new`, and `session/prompt` all succeed in Docker
  - probe result: `PROBE PASSED`, response text `Hello`

Review outcome:

- `#86` remains partial only because Claude is still quota/account-state
  blocked; Gemini Docker OAuth-backed certification is now fixed and verified.
- `.vaultspec` is no longer a supported mutable runtime sink for verifier
  bundles, service registry/logs, or gateway-managed worker stderr logs.
- One residual non-blocking follow-up remains explicit:
  project-level Gemini MCP extension discovery is noisy in the minimal worker
  image because not every referenced MCP runtime is installed there.

---

## Pass 16 Findings (2026-03-12 — `#86` Gemini verifier-path isolation hardening)

topic:

- `#86` Gemini Docker verifier-path isolation from workspace and user MCP noise

audit surface:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/providers/probes/certifying.py`
- `uv run vaultspec test prodlike-provider gemini`

rewrite scope:

- audit-loop entry plus the matching `#86` status updates in the continuous and
  consolidated audits

Scope:

- harden the already-passing Gemini Docker cert path so verifier evidence is
  auth-focused rather than polluted by workspace/user MCP discovery
- remove the outer certifying-wrapper timeout mismatch for interactive ACP
  providers
- close the remaining verifier-noise follow-up from Pass 15

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-39 | MEDIUM | Gemini Docker verifier noise | The verifier previously inherited repo workspace `.gemini/settings.json` plus the full mounted user `.gemini` state, which produced non-fatal `context7` / `nanobanana` MCP discovery noise in otherwise-passing Docker Gemini certification runs | Closed in `src/vaultspec_a2a/cli/_verify.py` by running the provider probe from `/tmp` and mounting a temp-backed minimal `GEMINI_CLI_HOME` containing only auth material plus `security.auth.selectedType`; the clean certifying bundle is `.vault/runtime/verify-prodlike-docker/20260312T084628Z` |
| AUD-71-40 | MEDIUM | interactive provider probe wrapper | The outer `src/vaultspec_a2a/providers/probes/certifying.py` wrapper could still hard-cut interactive ACP providers before their own auth watchdog resolved success, rejection, or cancellation | Fixed in `src/vaultspec_a2a/providers/probes/certifying.py` by removing the blanket outer timeout for interactive ACP providers like Gemini and Claude; focused coverage now locks that contract in `src/vaultspec_a2a/providers/probes/tests/test_certifying.py` |

Implementation evidence:

- `src/vaultspec_a2a/cli/_verify.py`
- `src/vaultspec_a2a/providers/probes/certifying.py`
- `src/vaultspec_a2a/cli/tests/test_verify.py`
- `src/vaultspec_a2a/providers/probes/tests/test_certifying.py`
- `.vault/runtime/verify-prodlike-docker/20260312T084628Z/gemini.probe.stdout.txt`
- `.vault/runtime/verify-prodlike-docker/20260312T084628Z/gemini.probe.json`
- `.vault/runtime/verify-prodlike-docker/20260312T084628Z/evidence-manifest.json`

Verification completed:

- `uv run ruff check src/vaultspec_a2a/cli/_verify.py src/vaultspec_a2a/providers/probes/certifying.py src/vaultspec_a2a/cli/tests/test_verify.py src/vaultspec_a2a/providers/probes/tests/test_certifying.py`
- `uv run python -m pytest src/vaultspec_a2a/cli/tests/test_verify.py src/vaultspec_a2a/providers/probes/tests/test_certifying.py -q`
- `uv run vaultspec test prodlike-provider gemini`
- reported result:
  - verifier probe executes from `/tmp`, not the repo root
  - mounted Gemini CLI state is reduced to auth-only material for the Docker
    cert path
  - `context7` / `nanobanana` verifier noise is gone from the fresh success
    bundle
  - Gemini ACP `initialize`, `session/new`, and `session/prompt` still pass in
    Docker

Review outcome:

- Code review surfaced one low-severity cleanup gap in the new temp-backed
  Gemini auth-home isolation path; it was fixed in the same slice by adding an
  explicit process-exit cleanup hook in `src/vaultspec_a2a/cli/_verify.py`.
- `#86` remains partial only because Claude is still blocked by provider
  quota/account state.
- The Docker Gemini certification surface is now both passing and cleanly
  isolated from workspace/user MCP noise during verifier runs.

---

## Pass 17 Findings (2026-03-12 — `#89` ACP interactive auth outcome surfacing)

topic:

- `#89` ACP interactive auth outcome surfacing for browser-driven Gemini auth
  handoff and operator evidence

audit surface:

- `src/vaultspec_a2a/providers/acp_chat_model.py`
- `src/vaultspec_a2a/providers/tests/test_acp_chat_model.py`
- `uv run python -m vaultspec_a2a.providers.probes.gemini`

rewrite scope:

- audit-loop entry plus the matching current-status updates in the continuous
  and consolidated audits

Scope:

- make the ACP authenticate wait path resolve on real auth outcomes rather than
  treating interactive browser auth as a generic timeout-only surface
- capture the browser OAuth URL from ACP stderr when Gemini CLI emits it
- preserve the already-working host-local Gemini ACP path while improving
  failure evidence

Findings captured in this pass:

| ID | Severity | Area | Finding | Resolution |
|----|----------|------|---------|------------|
| AUD-71-41 | MEDIUM | `#89` ACP interactive auth evidence | The ACP client could now distinguish auth-response vs subprocess-exit vs watchdog-timeout at the wait layer, but interactive browser-auth failures still collapsed into low-context errors that did not surface the actual OAuth handoff URL emitted by Gemini CLI | Fixed in `src/vaultspec_a2a/providers/acp_chat_model.py` by capturing browser-auth prompt state plus the follow-up OAuth URL from ACP stderr and appending that URL to timeout and early-exit `AcpAuthError` paths |
| AUD-71-42 | LOW | `#89` auth outcome taxonomy verification | The auth seam now distinguishes bounded machine-readable outcomes in `AcpAuthError.data`, including `watchdog_expired`, `operator_cancelled`, `auth_rejected`, `subprocess_exited_before_auth`, and fallback `auth_failed`, but focused async verification of the follow-on slice is currently blocked by a host-level Windows interpreter failure importing `asyncio` (`OSError: [WinError 10106] ... _overlapped`) | Narrowed under `#89`; taxonomy implementation is now in `src/vaultspec_a2a/providers/acp_chat_model.py`, `ruff` passed on the touched auth files, and the remaining blocker is host-runtime verification rather than an identified repo assertion failure |

Implementation evidence:

- `src/vaultspec_a2a/providers/acp_chat_model.py`
- `src/vaultspec_a2a/providers/tests/test_acp_chat_model.py`

Verification completed:

- `uv run ruff check src/vaultspec_a2a/providers/acp_chat_model.py src/vaultspec_a2a/providers/tests/test_acp_chat_model.py`
- `uv run python -m pytest src/vaultspec_a2a/providers/tests/test_acp_chat_model.py -q`
- `uv run python -m vaultspec_a2a.providers.probes.gemini`
- reported result:
  - browser-auth prompt lines are captured from ACP stderr
  - captured OAuth URLs are surfaced in auth timeout / early-exit error text
  - focused coverage passes with `14 passed, 4 deselected`
  - the host-local authenticated Gemini ACP path still passes end to end:
    `initialize`, `session/new`, and `session/prompt`

Grounding:

- Official Gemini CLI docs continue to support the implemented model:
  browser-based Google login on a local machine, cached credentials for future
  sessions, `GEMINI_CLI_HOME` as the persisted state root, and cached-cred/env
  auth for headless or containerized paths.
- Sources:
  - `https://github.com/google-gemini/gemini-cli/blob/main/docs/get-started/authentication.md`
  - `https://github.com/google-gemini/gemini-cli/blob/main/docs/reference/configuration.md`
  - `https://github.com/google-gemini/gemini-cli/blob/main/docs/tools/mcp-server.md`
  - Context7 `/google-gemini/gemini-cli`

Review outcome:

- Code review on the actual diff found no remaining defect in the final slice.
- `#89` advances on operator-facing auth evidence without changing the already
  certified Docker Gemini auth-material contract under `#86`.
- Follow-on `#89` hardening now adds machine-readable
  `AcpAuthError.data["auth_outcome"]` classification for watchdog expiry,
  operator cancellation, auth rejection, subprocess-exit-before-auth, and
  generic fallback auth failure.
- Focused async verification for that exact follow-on slice is currently
  blocked by a host-level Windows interpreter failure importing `asyncio`
  (`OSError: [WinError 10106] ... _overlapped`), not by a recorded repo
  assertion failure.
