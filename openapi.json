{
  "openapi": "3.1.0",
  "info": {
    "title": "Vaultspec A2A Orchestrator",
    "version": "0.3.0"
  },
  "paths": {
    "/admin/shutdown": {
      "post": {
        "summary": "Shutdown Endpoint",
        "description": "Initiate graceful server shutdown.\n\nAdministrative shutdown is a receipt-bound lifecycle operation: it requires\nboth authenticated runtime control (the attach credential) and receipt\nownership (the lifecycle capability that discovery never references), so a\nforeign attachment that can reach the product surface still cannot stop the\ngateway.\n\nRun admission is closed first, so the gateway admits no new run while it\ndrains; the in-process stop is then deferred briefly so this 202 flushes\nbefore the SIGINT-driven graceful shutdown - which drains and reaps the owned\nworker and run descendants in the lifespan - begins.",
        "operationId": "shutdown_endpoint_admin_shutdown_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "X-Vaultspec-Lifecycle-Capability",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "X-Vaultspec-Lifecycle-Capability"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Shutdown Endpoint Admin Shutdown Post"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "403": {
            "description": "Missing or invalid lifecycle capability receipt."
          },
          "503": {
            "description": "Gateway service token or lifecycle capability is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs": {
      "post": {
        "summary": "Run Start Endpoint",
        "description": "Start, prepare, commit, or release through the single run-start verb.\n\nThe ``stage`` selector splits one verb into four shapes without growing the\nverb set: ``prepare`` reserves bounded capacity without tokens or a durable\nrun; ``commit`` binds the exact actor-token role set to that reservation;\n``release`` frees only an uncommitted reservation; and ``start`` (the\ndefault) preserves the one-shot engine/Compose path.",
        "operationId": "run_start_endpoint_v1_runs_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunStartRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/RunStartResponse"
                    },
                    {
                      "$ref": "#/components/schemas/RunPrepareResponse"
                    },
                    {
                      "$ref": "#/components/schemas/RunCommitResponse"
                    },
                    {
                      "$ref": "#/components/schemas/RunReleaseResponse"
                    }
                  ],
                  "title": "Response Run Start Endpoint V1 Runs Post"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "get": {
        "summary": "Active Runs Endpoint",
        "description": "List runs: non-terminal by default, or every run including terminal ones.\n\nThe default is unchanged and remains the capped identity projection of\ndurable non-terminal runs that the engine contract certified - a caller that\npasses nothing sees exactly what it saw before, byte for byte.\n\n``state=all`` is the history read, and it differs from discovery in more than\nwhich rows it returns. It answers through the paginated list service rather\nthan the discovery one, because discovery exists to find live work and is\ncapped for that purpose while history has to walk a store that only grows;\nthat is why this mode carries a total and an offset. And it answers with a\nWIDER record, because the two readings are asked different questions: a\nviewer binding to live work needs an identity, while a reader of history is\nasking what happened and needs the projection that separates a healthy run\nfrom a degraded one.\n\nThe two shapes are returned as two models rather than one union, so widening\nhistory cannot perturb a single byte of the certified discovery response.",
        "operationId": "active_runs_endpoint_v1_runs_get",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "state",
            "in": "query",
            "required": false,
            "schema": {
              "enum": [
                "active",
                "all"
              ],
              "type": "string",
              "default": "active",
              "title": "State"
            }
          },
          {
            "name": "workspace_root",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 4096
                },
                {
                  "type": "null"
                }
              ],
              "title": "Workspace Root"
            }
          },
          {
            "name": "feature_tag",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 128
                },
                {
                  "type": "null"
                }
              ],
              "title": "Feature Tag"
            }
          },
          {
            "name": "status",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/ThreadStatus"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Status"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 100,
              "minimum": 1,
              "default": 50,
              "title": "Limit"
            }
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "default": 0,
              "title": "Offset"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The discovery reading for ``state=active`` and the wider history reading for ``state=all``.",
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/ActiveRunsResponse"
                    },
                    {
                      "$ref": "#/components/schemas/RunSummariesResponse"
                    }
                  ],
                  "title": "Response 200 Active Runs Endpoint V1 Runs Get"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}": {
      "get": {
        "summary": "Run Status Endpoint",
        "description": "Return the authoritative recovery snapshot for a run.",
        "operationId": "run_status_endpoint_v1_runs__run_id__get",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunStatusResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      },
      "delete": {
        "summary": "Run Delete Endpoint",
        "description": "Delete a run through the durable cross-store deletion saga.\n\nA replayed request resumes the same saga rather than starting a second\nteardown, so repeated calls converge on one deletion.\n\nFive outcomes, because the service distinguishes more states than two codes\ncan carry: a lifecycle refusal before the saga begins, a clean deletion, a\ndeletion that finalized over unremovable state, resumable incomplete\ncleanup, and an already-absent run. The retryable code is reserved for the\ngenuinely resumable case - the abandoned case is terminal, and inviting a\nretry there would send the caller to a not-found.",
        "operationId": "run_delete_endpoint_v1_runs__run_id__delete",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted; every store was cleaned."
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Cleanup is unfinished but resumable; retry."
          },
          "200": {
            "description": "Deleted, but cleanup was abandoned over permanently unremovable state; the body names the kinds left behind.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunDeleteResponse"
                }
              }
            }
          },
          "404": {
            "description": "No such run."
          },
          "409": {
            "description": "The run's lifecycle state refuses deletion."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}/stream": {
      "get": {
        "summary": "Run Stream Endpoint",
        "description": "Re-serve the run's bounded, versioned v1 SSE progress frames.\n\nThe public streaming companion to run-status: run-status is the authoritative\nrecovery snapshot, this is the droppable live progress relay. A run id is the\nthread id, so this delegates to the same stream builder the internal\nprogress stream has always used - one code path, the same versioned\n256 KiB-bounded frames, the same terminal-replay-then-close semantics. Frames\nare non-authoritative by contract: a consumer reconciles run state from\nrun-status, never from a relay frame.",
        "operationId": "run_stream_endpoint_v1_runs__run_id__stream_get",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}/cancel": {
      "post": {
        "summary": "Run Cancel Endpoint",
        "description": "Cancel a run idempotently.",
        "operationId": "run_cancel_endpoint_v1_runs__run_id__cancel_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunCancelResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}/history": {
      "get": {
        "summary": "Run History Endpoint",
        "description": "Read one run whole, including a terminal or archived one.\n\nDistinct from run-status by design. Run-status is the BOUNDED recovery\nsnapshot an engine reconciles authority from, and widening it would have\nmade every reconciliation pay for a transcript it does not read. This is the\nwide read for a consumer that wants the record: transcript, agents, plan,\npending answers, and the run's metadata.\n\n\"Whole\" is a promise about honesty, not about always having everything. The\ntranscript lives only in the checkpoint, and a checkpoint can be gone or\nunreachable; when it is, this answers 200 with the durable half of the\nrecord it CAN read - status, agents, plan, permissions, metadata - and says\nplainly that the transcript is not part of it. Refusing the whole read over\nan absent transcript would cost the caller the half that survived, and\nanswering an unqualified empty message list would be worse still: silent\nloss dressed as a run that never spoke.\n\nThe state snapshot is embedded rather than restated, so this response cannot\ndrift from the snapshot it reports.",
        "operationId": "run_history_endpoint_v1_runs__run_id__history_get",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunHistoryResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}/archive": {
      "post": {
        "summary": "Run Archive Endpoint",
        "description": "Move a terminal run to the archived state.\n\nArchiving is not deletion: the run and its records survive, marked\nhistorical. A run whose state does not permit archiving is refused rather\nthan silently ignored, and repeating the call on an already-archived run is\nthat same refusal - the conflict IS the replay signal here.",
        "operationId": "run_archive_endpoint_v1_runs__run_id__archive_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunArchiveResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/team/status": {
      "get": {
        "summary": "Team Status Endpoint",
        "description": "Report the team's live operational projection.\n\nA read, and deliberately a narrow one: which agents exist and what state\nthey are in, which runs are active, and what is awaiting an answer. It\ncarries no prompt, no document body, and no credential - the same\ndisclosure discipline the progress channel holds.",
        "operationId": "team_status_endpoint_v1_team_status_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TeamStatusV1Response"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "GatewayServiceToken": []
          }
        ]
      }
    },
    "/v1/runs/{run_id}/messages": {
      "post": {
        "summary": "Run Message Endpoint",
        "description": "Send a follow-up turn into an existing run.\n\nRun-start cannot carry this. A repeat run identifier there is a REPLAY - it\nanswers with the original run and never adopts the new body - so without\nthis verb the versioned surface can start a run and watch it, but never say\nanything further to it.\n\nAccepted is not applied: the turn is handed to the worker and execution\ncontinues asynchronously, so a caller reconciles from the stream or\nrun-status rather than from this response.",
        "operationId": "run_message_endpoint_v1_runs__run_id__messages_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunMessageRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunMessageResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}/permissions/{request_id}/respond": {
      "post": {
        "summary": "Run Permission Respond Endpoint",
        "description": "Answer a permission request the run raised on its progress stream.\n\nThe versioned surface already POSES this question - ``permission_request``\nis an enumerated frame on run-stream - and this is where the answer returns.\nWithout it the only answering channel is the transition surface, so retiring\nthat surface would strand every paused run.\n\nThe verb adds no state machine of its own: it is a versioned projection of\nthe same answer path, so at-most-once behaviour comes from there. Answering\ntwice replays the stored outcome rather than acting again, an answer arriving\nafter the request was applied reports the duplicate without re-dispatching,\nand a superseded or expired request is refused with a journaled rejection\nthat replays identically.\n\nScoping matters as much as the answer. The request is resolved and checked\nagainst the run in the path BEFORE anything acts on it, so a guessed request\nid cannot be used to answer another run's question - and because that check\nprecedes the service call, a mismatch has no effect at all rather than being\ndetected after the fact.",
        "operationId": "run_permission_respond_endpoint_v1_runs__run_id__permissions__request_id__respond_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          },
          {
            "name": "request_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Request Id"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunPermissionRespondRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunPermissionRespondResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}/clarifications/{request_id}/respond": {
      "post": {
        "summary": "Run Clarification Respond Endpoint",
        "description": "Resolve through the durable clarification lease service.",
        "operationId": "run_clarification_respond_endpoint_v1_runs__run_id__clarifications__request_id__respond_post",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 128,
              "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
              "title": "Run Id"
            }
          },
          {
            "name": "request_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Request Id"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunClarificationRespondRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunClarificationRespondResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/provider-catalog": {
      "get": {
        "summary": "Provider Catalog Endpoint",
        "description": "Serve prompt-free, execution-lane-specific catalogs for one workspace.",
        "operationId": "provider_catalog_endpoint_v1_provider_catalog_get",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "workspace_root",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 4096,
              "title": "Workspace Root"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProviderCatalogResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/presets": {
      "get": {
        "summary": "Presets List Endpoint",
        "description": "List team presets truthfully, marking each loadable or unloadable.\n\nResolution uses the requested workspace context so workspace-local presets\nare listed alongside the bundled set. A single preset that fails to load or\nvalidate is reported with ``loadable=False`` and a reason rather than\nomitted or allowed to crash the whole listing. Each loadable preset carries\nits model profiles with per-role effective assignments (resolved by the same\nshared resolver launch uses) and backend-computed eligibility. The whole\nbuild - file I/O, provider readiness, and the engine reachability probe -\nruns off the event loop.",
        "operationId": "presets_list_endpoint_v1_presets_get",
        "security": [
          {
            "GatewayServiceToken": []
          }
        ],
        "parameters": [
          {
            "name": "workspace_root",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "maxLength": 4096
                },
                {
                  "type": "null"
                }
              ],
              "title": "Workspace Root"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PresetsListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        }
      }
    },
    "/v1/service": {
      "get": {
        "summary": "Service State Endpoint",
        "description": "Return truthful, probe-backed readiness for the resident gateway.\n\nRuns the real dependency probes (database, checkpoint, worker) rather than\nreporting a hardcoded status, and separates process-alive from\ncan-accept-run. Engine authoring-backend reachability is reported from\nnon-blocking discovery-file freshness.",
        "operationId": "service_state_endpoint_v1_service_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ServiceStateResponse"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid gateway service token."
          },
          "503": {
            "description": "Gateway service token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "GatewayServiceToken": []
          }
        ]
      }
    },
    "/internal/health": {
      "get": {
        "tags": [
          "internal"
        ],
        "summary": "Internal Health",
        "description": "Readiness probe -- confirms the internal API is accepting connections.",
        "operationId": "internal_health_internal_health_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object",
                  "title": "Response Internal Health Internal Health Get"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid internal IPC token."
          },
          "500": {
            "description": "Internal IPC token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "InternalIpcToken": []
          }
        ]
      }
    },
    "/internal/events": {
      "post": {
        "tags": [
          "internal"
        ],
        "summary": "Receive Worker Event",
        "description": "Receive a single event from the worker and relay to browser clients.\n\nThe ``WorkerBridge`` in the worker process POSTs here instead of\nsending a WebSocket frame.  The payload format matches\n``WorkerEventEnvelope``.",
        "operationId": "receive_worker_event_internal_events_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object",
                  "title": "Response Receive Worker Event Internal Events Post"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid internal IPC token."
          },
          "500": {
            "description": "Internal IPC token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "InternalIpcToken": []
          }
        ]
      }
    },
    "/internal/events/batch": {
      "post": {
        "tags": [
          "internal"
        ],
        "summary": "Receive Worker Event Batch",
        "description": "Receive a batch of events from the worker.\n\nThe ``WorkerBridge`` accumulates events for a short interval then\nPOSTs them as a single ``{\"events\": [...]}`` payload.  Each entry\nhas the same shape as a single-event envelope (``thread_id`` +\n``payload``).",
        "operationId": "receive_worker_event_batch_internal_events_batch_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object",
                  "title": "Response Receive Worker Event Batch Internal Events Batch Post"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid internal IPC token."
          },
          "500": {
            "description": "Internal IPC token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "InternalIpcToken": []
          }
        ]
      }
    },
    "/internal/heartbeat": {
      "post": {
        "tags": [
          "internal"
        ],
        "summary": "Receive Worker Heartbeat",
        "description": "Receive a heartbeat from the worker.\n\nUpdates ``app.state`` so the gateway can monitor worker\nliveness without a persistent WebSocket connection.",
        "operationId": "receive_worker_heartbeat_internal_heartbeat_post",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "type": "object",
                  "title": "Response Receive Worker Heartbeat Internal Heartbeat Post"
                }
              }
            }
          },
          "401": {
            "description": "Missing or invalid internal IPC token."
          },
          "500": {
            "description": "Internal IPC token is not configured."
          },
          "422": {
            "description": "Validation Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            }
          }
        },
        "security": [
          {
            "InternalIpcToken": []
          }
        ]
      }
    },
    "/health": {
      "get": {
        "summary": "Health Endpoint",
        "description": "Top-level health check: the one probe surface for external callers.\n\nUnder the armed desktop profile the unauthenticated boundary discloses\nonly the minimal liveness fact - no process identity, product identity, or\nproduct state - while an attach-authenticated caller additionally receives\nthe readiness projection from the single readiness authority, computed\nover a live database probe. Only the authenticated branch probes: the\nunauthenticated answer stays a constant so it cannot be used to measure\nthe gateway's dependencies.\n\nBoth profiles PROBE rather than read off app state - the DB here and, on\nCompose and development, the checkpointer and worker as well - because a\nhealthcheck that only reports what the process believes about itself\ncannot notice a dependency that has gone away.",
        "operationId": "health_endpoint_health_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": true,
                  "type": "object",
                  "title": "Response Health Endpoint Health Get"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ActiveRunRecord": {
        "properties": {
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "$ref": "#/components/schemas/ThreadStatus"
          },
          "feature_tag": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128
              },
              {
                "type": "null"
              }
            ],
            "title": "Feature Tag"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "status"
        ],
        "title": "ActiveRunRecord",
        "description": "Minimal durable run identity used to recover a viewing binding."
      },
      "ActiveRunsResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "state": {
            "type": "string",
            "enum": [
              "active",
              "all"
            ],
            "title": "State",
            "default": "active"
          },
          "runs": {
            "items": {
              "$ref": "#/components/schemas/ActiveRunRecord"
            },
            "type": "array",
            "maxItems": 100,
            "title": "Runs"
          },
          "truncated": {
            "type": "boolean",
            "title": "Truncated",
            "default": false
          },
          "total": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total"
          }
        },
        "type": "object",
        "title": "ActiveRunsResponse",
        "description": "Bounded, non-authoritative run listing.\n\n``state`` distinguishes the two readings deliberately. The default is the\ncapped discovery projection of non-terminal runs - what the engine contract\ncertified, unchanged. ``all`` is the history read over the paginated store,\nwhich is the only mode that can report a ``total``: discovery caps its answer\nby design and has no honest total to give."
      },
      "ActorTokenBundle": {
        "properties": {
          "tokens": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object",
            "title": "Tokens"
          },
          "engine_bearer": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Engine Bearer"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "ActorTokenBundle",
        "description": "A run's per-role actor tokens plus the engine machine bearer.\n\nParameters\n----------\ntokens:\n    Mapping of role identifier (the worker ``agent_id`` or the supervisor\n    constant) to that role's engine-minted actor token. A role only ever\n    receives its own token via :meth:`actor_token`.\nengine_bearer:\n    The machine bearer minted at engine boot, forwarded so a worker's\n    authoring bridge can reach the engine. Optional in the wire shape only:\n    a run that carries no bearer leaves the bridge unarmed, because the\n    bearer is meaningful only paired with the engine origin it was minted\n    for. A consumer that resolves its OWN origin may substitute the bearer\n    from that same resolution (the executor's run-end session close does);\n    a consumer holding a separately-supplied origin must not, since pairing\n    a bearer with a foreign origin would misdirect the credential."
      },
      "AdmissionState": {
        "type": "string",
        "enum": [
          "admitted",
          "not_admitted",
          "unknown"
        ],
        "title": "AdmissionState",
        "description": "Completed-turn admission evidence for an execution lane."
      },
      "AgentLifecycleState": {
        "type": "string",
        "enum": [
          "submitted",
          "idle",
          "working",
          "input_required",
          "auth_required",
          "completed",
          "failed",
          "cancelled"
        ],
        "title": "AgentLifecycleState",
        "description": "Observable agent states exposed to the frontend.\n\nMaps to the MCP states. Tracks\ninternal process lifecycle (init/ready/running/error/done)."
      },
      "AgentSnapshot": {
        "properties": {
          "agent_id": {
            "type": "string",
            "title": "Agent Id"
          },
          "node_name": {
            "type": "string",
            "title": "Node Name"
          },
          "state": {
            "$ref": "#/components/schemas/AgentLifecycleState"
          },
          "provider": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Provider"
              },
              {
                "type": "null"
              }
            ]
          },
          "model": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Model"
              },
              {
                "type": "null"
              }
            ]
          },
          "model_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Model Name"
          },
          "role": {
            "type": "string",
            "title": "Role",
            "default": ""
          },
          "display_name": {
            "type": "string",
            "title": "Display Name",
            "default": ""
          },
          "description": {
            "type": "string",
            "title": "Description",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "node_name",
          "state"
        ],
        "title": "AgentSnapshot",
        "description": "Wire projection of the canonical agent descriptor within a snapshot.\n\nMirrors ``thread.snapshots.AgentData``."
      },
      "ApprovalStatus": {
        "type": "string",
        "enum": [
          "pending",
          "approved",
          "rejected",
          "superseded"
        ],
        "title": "ApprovalStatus",
        "description": "Durable lifecycle for plan approval state on a thread."
      },
      "ArtifactSnapshot": {
        "properties": {
          "artifact_id": {
            "type": "string",
            "title": "Artifact Id"
          },
          "filename": {
            "type": "string",
            "title": "Filename"
          },
          "content": {
            "type": "string",
            "title": "Content"
          },
          "complete": {
            "type": "boolean",
            "title": "Complete"
          }
        },
        "type": "object",
        "required": [
          "artifact_id",
          "filename",
          "content",
          "complete"
        ],
        "title": "ArtifactSnapshot",
        "description": "Fully materialized file artifact."
      },
      "AssignmentSource": {
        "type": "string",
        "enum": [
          "profile",
          "worker",
          "agent",
          "team_default",
          "undeclared"
        ],
        "title": "AssignmentSource",
        "description": "Which precedence layer a resolved value came from (ADR exposure field).\n\n``UNDECLARED`` is the honest bottom of the chain: no layer supplied the value.\nIt exists because the alternative was reporting a repository-authored fallback\nas ``TEAM_DEFAULT`` - a label asserting a preset declared something it never\ndeclared, which is a worse failure than an absent value because it cannot be\ntold apart from a real declaration."
      },
      "AuthenticationState": {
        "type": "string",
        "enum": [
          "authenticated",
          "unauthenticated",
          "unknown",
          "not_applicable"
        ],
        "title": "AuthenticationState",
        "description": "Authentication evidence, separate from configuration presence."
      },
      "AuthoringCapability": {
        "type": "string",
        "enum": [
          "coding",
          "document_authoring"
        ],
        "title": "AuthoringCapability",
        "description": "The coarse kind of work a preset delivers.\n\nA two-member split rather than a spectrum: a consumer branches on whether a\npreset produces vault documents or source changes, and nothing served today\ndistinguishes finer than that.\n\nNaming what this does NOT decide, because the field's name invites the\nassumption: this is a DESCRIPTION of a preset, not a gate on one. Nothing in\nthe run-admission path reads it - the submitter gate reaches the same\nunderlying question through its own derivation - so a wrong value here\nmisinforms a reader and refuses nobody."
      },
      "CatalogStatus": {
        "type": "string",
        "enum": [
          "available",
          "unavailable",
          "stale",
          "unknown"
        ],
        "title": "CatalogStatus",
        "description": "Provider catalog availability and freshness."
      },
      "ClarificationKind": {
        "type": "string",
        "enum": [
          "choice",
          "text"
        ],
        "title": "ClarificationKind",
        "description": "How one question is answered.\n\n``choice`` offers a bounded set of declared options and admits exactly one of\nthem verbatim as its answer; ``text`` admits free prose up to\n:data:`MAX_ANSWER_CHARS`. The two exhaust the vocabulary: a kind the renderer\ndoes not know is a question it cannot render, so the set is closed."
      },
      "ClarificationQuestion": {
        "properties": {
          "id": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.\\-]*$",
            "title": "Id"
          },
          "prompt": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "title": "Prompt"
          },
          "kind": {
            "$ref": "#/components/schemas/ClarificationKind"
          },
          "options": {
            "anyOf": [
              {
                "items": {
                  "type": "string",
                  "maxLength": 128,
                  "minLength": 1
                },
                "type": "array",
                "maxItems": 4
              },
              {
                "type": "null"
              }
            ],
            "title": "Options"
          },
          "required": {
            "type": "boolean",
            "title": "Required",
            "default": true
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "id",
          "prompt",
          "kind"
        ],
        "title": "ClarificationQuestion",
        "description": "One bounded question inside a clarification request.\n\n``options`` belongs to ``choice`` and only to ``choice``: a text question\ncarrying options would advertise a constraint the answer path does not\nenforce, and a choice without options would offer nothing to choose. Both are\nrefused at construction rather than tolerated and interpreted downstream."
      },
      "ClarificationQuestionSnapshot": {
        "properties": {
          "id": {
            "type": "string",
            "title": "Id"
          },
          "prompt": {
            "type": "string",
            "title": "Prompt"
          },
          "kind": {
            "type": "string",
            "title": "Kind"
          },
          "required": {
            "type": "boolean",
            "title": "Required",
            "default": false
          },
          "options": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Options"
          }
        },
        "type": "object",
        "required": [
          "id",
          "prompt",
          "kind"
        ],
        "title": "ClarificationQuestionSnapshot",
        "description": "Layer 1 equivalent of ``thread.snapshots.ClarificationQuestionData``."
      },
      "ClarificationRequest": {
        "properties": {
          "type": {
            "type": "string",
            "const": "clarification_request",
            "title": "Type",
            "default": "clarification_request"
          },
          "request_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.\\-]*$",
            "title": "Request Id"
          },
          "questions": {
            "items": {
              "$ref": "#/components/schemas/ClarificationQuestion"
            },
            "type": "array",
            "maxItems": 4,
            "minItems": 1,
            "title": "Questions"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "request_id",
          "questions"
        ],
        "title": "ClarificationRequest",
        "description": "The bounded question set a parked run is waiting on.\n\nThis is the interrupt payload verbatim: what the node raises, what the\ncheckpoint holds while the run is parked, and what the recovery snapshot\ndiscloses. ``type`` is carried rather than implied because the checkpoint\nholds interrupts of several kinds side by side and the discriminator is how a\nreader tells a clarification from an approval."
      },
      "ClarificationRequestSnapshot": {
        "properties": {
          "request_id": {
            "type": "string",
            "title": "Request Id"
          },
          "questions": {
            "items": {
              "$ref": "#/components/schemas/ClarificationQuestionSnapshot"
            },
            "type": "array",
            "title": "Questions"
          }
        },
        "type": "object",
        "required": [
          "request_id"
        ],
        "title": "ClarificationRequestSnapshot",
        "description": "Layer 1 equivalent of ``thread.snapshots.ClarificationRequestData``.\n\nA pending mid-run clarification, disclosed on ``run-status`` so a reload\nre-renders the questionnaire from authoritative state alone, never from a\nrelay frame."
      },
      "CleanupKind": {
        "type": "string",
        "enum": [
          "checkpoint",
          "artifact_file"
        ],
        "title": "CleanupKind",
        "description": "The kind of thread-owned state one deletion cleanup item removes.\n\nDeliberately a kind and never a locator: it names *what class of state* a\ndelete touched, so both the durable cleanup manifest and the delete\nresponse can speak it, while the concrete checkpoint id or artifact path\nstays inside the control plane."
      },
      "ContextRef": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "stage": {
            "type": "string",
            "title": "Stage"
          },
          "summary": {
            "type": "string",
            "title": "Summary",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "path",
          "stage"
        ],
        "title": "ContextRef",
        "description": "Reference to a context document in the .vault hierarchy."
      },
      "ControlKind": {
        "type": "string",
        "enum": [
          "model_config",
          "thought_level",
          "service_tier",
          "other"
        ],
        "title": "ControlKind",
        "description": "Provider-native control category without cross-provider semantics."
      },
      "DesktopReadiness": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "gateway_pid": {
            "type": "integer",
            "title": "Gateway Pid"
          },
          "generation": {
            "type": "string",
            "title": "Generation"
          },
          "profile": {
            "type": "string",
            "title": "Profile"
          },
          "liveness": {
            "$ref": "#/components/schemas/LivenessState",
            "default": "alive"
          },
          "gateway_readiness": {
            "$ref": "#/components/schemas/GatewayReadiness"
          },
          "worker_state": {
            "$ref": "#/components/schemas/WorkerLifecycleState"
          },
          "provider_eligibility": {
            "$ref": "#/components/schemas/ProviderEligibility"
          },
          "eligible_providers": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 16,
            "title": "Eligible Providers"
          },
          "run_admission": {
            "$ref": "#/components/schemas/RunAdmission"
          },
          "reasons": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 16,
            "title": "Reasons"
          }
        },
        "type": "object",
        "required": [
          "gateway_pid",
          "generation",
          "profile",
          "gateway_readiness",
          "worker_state",
          "provider_eligibility",
          "run_admission"
        ],
        "title": "DesktopReadiness",
        "description": "The authenticated desktop readiness projection.\n\nCarries process and product identity alongside the five separate bounded\nreadiness facts. The facts are never collapsed into a single boolean: a cold,\nstartable worker leaves ``gateway_readiness`` ``ready`` while ``run_admission``\nstays ``deferred``, so gateway-readiness and execution-readiness remain\ndistinct. Served only to an attach-authenticated caller."
      },
      "DocumentCapability": {
        "type": "string",
        "enum": [
          "research_document",
          "architecture_decision",
          "plan_document"
        ],
        "title": "DocumentCapability",
        "description": "One concrete vault document a preset's topology can produce.\n\nDistinct from :class:`AuthoringCapability`, which says only WHETHER a preset\nauthors documents; this says WHICH. The two are served side by side and a\nreader needs both, so they are not collapsible: a preset can be\ndocument-authoring while producing a set this vocabulary does not yet name.\n\nMembers follow the research_adr phase machine's three gated outputs. The\nvocabulary is deliberately narrower than the set of documents the wider\nsystem knows about - an audit or a reference record has no producing\ntopology here, and a member with no producer would advertise a deliverable\nno preset can deliver."
      },
      "ExecutionTaskSnapshot": {
        "properties": {
          "task_id": {
            "type": "string",
            "title": "Task Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "path": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Path"
          },
          "has_error": {
            "type": "boolean",
            "title": "Has Error",
            "default": false
          },
          "error_type": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Error Type"
          },
          "interrupt_ids": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Interrupt Ids"
          },
          "interrupt_types": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Interrupt Types"
          },
          "has_nested_state": {
            "type": "boolean",
            "title": "Has Nested State",
            "default": false
          },
          "has_result": {
            "type": "boolean",
            "title": "Has Result",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "task_id",
          "name"
        ],
        "title": "ExecutionTaskSnapshot",
        "description": "Normalized execution task used in reconnect snapshots."
      },
      "FrozenExecutionSnapshotSummary": {
        "properties": {
          "provider_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Provider Id"
          },
          "provider_display_name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Provider Display Name"
          },
          "execution_mode": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Execution Mode"
          },
          "catalog_revision": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Catalog Revision"
          },
          "entry_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Entry Id"
          },
          "model_name": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Model Name"
          },
          "model_display_name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Model Display Name"
          },
          "controls": {
            "items": {
              "$ref": "#/components/schemas/FrozenNativeControlSummary"
            },
            "type": "array",
            "maxItems": 32,
            "title": "Controls"
          }
        },
        "type": "object",
        "required": [
          "provider_id",
          "execution_mode",
          "catalog_revision",
          "entry_id",
          "model_name"
        ],
        "title": "FrozenExecutionSnapshotSummary",
        "description": "Catalog provenance and exact provider inputs for one execution lane."
      },
      "FrozenNativeControlSummary": {
        "properties": {
          "control_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Control Id"
          },
          "option_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Option Id"
          },
          "provider_value": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Provider Value"
          },
          "display_name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Display Name"
          },
          "option_display_name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Option Display Name"
          }
        },
        "type": "object",
        "required": [
          "control_id",
          "option_id",
          "provider_value"
        ],
        "title": "FrozenNativeControlSummary",
        "description": "One exact provider-native value frozen for historical disclosure."
      },
      "FrozenRoleAssignmentSummary": {
        "properties": {
          "provider_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Provider Id"
          },
          "provider_display_name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Provider Display Name"
          },
          "execution_mode": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Execution Mode"
          },
          "catalog_revision": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Catalog Revision"
          },
          "entry_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Entry Id"
          },
          "model_name": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Model Name"
          },
          "model_display_name": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Model Display Name"
          },
          "controls": {
            "items": {
              "$ref": "#/components/schemas/FrozenNativeControlSummary"
            },
            "type": "array",
            "maxItems": 32,
            "title": "Controls"
          },
          "role_id": {
            "type": "string",
            "maxLength": 1024,
            "minLength": 1,
            "title": "Role Id"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1024
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "fallbacks": {
            "items": {
              "$ref": "#/components/schemas/FrozenExecutionSnapshotSummary"
            },
            "type": "array",
            "maxItems": 8,
            "title": "Fallbacks"
          },
          "provenance": {
            "$ref": "#/components/schemas/FrozenSelectionProvenanceSummary"
          }
        },
        "type": "object",
        "required": [
          "provider_id",
          "execution_mode",
          "catalog_revision",
          "entry_id",
          "model_name",
          "role_id",
          "provenance"
        ],
        "title": "FrozenRoleAssignmentSummary",
        "description": "One role's exact primary and ordered fallback execution snapshots."
      },
      "FrozenSelectionProvenanceSummary": {
        "properties": {
          "selection_source": {
            "type": "string",
            "enum": [
              "team_selection",
              "role_override"
            ],
            "title": "Selection Source"
          }
        },
        "type": "object",
        "required": [
          "selection_source"
        ],
        "title": "FrozenSelectionProvenanceSummary",
        "description": "Bounded admission layer that supplied one role's primary lane."
      },
      "FrozenTeamAssignmentSummary": {
        "properties": {
          "schema_version": {
            "type": "integer",
            "const": 1,
            "title": "Schema Version",
            "default": 1
          },
          "digest": {
            "type": "string",
            "maxLength": 71,
            "minLength": 64,
            "pattern": "^(?:sha256:)?[a-f0-9]{64}$",
            "title": "Digest"
          },
          "assignments": {
            "items": {
              "$ref": "#/components/schemas/FrozenRoleAssignmentSummary"
            },
            "type": "array",
            "maxItems": 64,
            "minItems": 1,
            "title": "Assignments"
          }
        },
        "type": "object",
        "required": [
          "digest",
          "assignments"
        ],
        "title": "FrozenTeamAssignmentSummary",
        "description": "Complete immutable schema-v1 execution authority for a modern run."
      },
      "GatewayReadiness": {
        "type": "string",
        "enum": [
          "ready",
          "not_ready"
        ],
        "title": "GatewayReadiness",
        "description": "Whether a live gateway with a valid database is ready to be attached.\n\nIndependent of worker state: a live gateway with a valid database and a cold,\nstartable worker is ``ready``. A gateway-owned dependency failure - an invalid\nor unreachable database - is ``not_ready``."
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "HealthState": {
        "type": "string",
        "enum": [
          "available",
          "unavailable",
          "unknown"
        ],
        "title": "HealthState",
        "description": "A factual binary health axis whose evidence may still be unknown."
      },
      "LivenessState": {
        "type": "string",
        "enum": [
          "alive",
          "not_alive"
        ],
        "title": "LivenessState",
        "description": "Minimal process-liveness fact: the gateway answered, or it did not.\n\nThis is the only fact an unauthenticated caller ever observes. It proves\nneither ownership nor readiness and discloses no product state."
      },
      "MessageSnapshot": {
        "properties": {
          "message_id": {
            "type": "string",
            "title": "Message Id"
          },
          "role": {
            "type": "string",
            "title": "Role"
          },
          "content": {
            "type": "string",
            "title": "Content"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "timestamp": {
            "type": "string",
            "format": "date-time",
            "title": "Timestamp"
          }
        },
        "type": "object",
        "required": [
          "message_id",
          "role",
          "content",
          "timestamp"
        ],
        "title": "MessageSnapshot",
        "description": "Fully materialized message in a thread replay."
      },
      "Model": {
        "type": "string",
        "enum": [
          "low",
          "mid",
          "high",
          "max"
        ],
        "title": "Model",
        "description": "LLM capability levels.\n\nAbstracts specific version strings to reduce maintenance burden."
      },
      "PermissionOptionKind": {
        "type": "string",
        "enum": [
          "allow_once",
          "allow_always",
          "reject_once",
          "reject_always"
        ],
        "title": "PermissionOptionKind",
        "description": "User permission response options (mirrors ACP PermissionOption.kind).\n\nValues:\n    ALLOW_ONCE: Allow the tool call this time only.\n    ALLOW_ALWAYS: Allow all future invocations of this tool without prompting.\n    REJECT_ONCE: Deny the tool call this time only.\n    REJECT_ALWAYS: Deny all future invocations of this tool without prompting."
      },
      "PermissionOptionSnapshot": {
        "properties": {
          "option_id": {
            "type": "string",
            "title": "Option Id"
          },
          "name": {
            "type": "string",
            "title": "Name"
          },
          "kind": {
            "$ref": "#/components/schemas/PermissionOptionKind"
          }
        },
        "type": "object",
        "required": [
          "option_id",
          "name",
          "kind"
        ],
        "title": "PermissionOptionSnapshot",
        "description": "Permission option within a snapshot."
      },
      "PermissionSnapshot": {
        "properties": {
          "request_id": {
            "type": "string",
            "title": "Request Id"
          },
          "description": {
            "type": "string",
            "title": "Description"
          },
          "options": {
            "items": {
              "$ref": "#/components/schemas/PermissionOptionSnapshot"
            },
            "type": "array",
            "title": "Options"
          },
          "tool_call": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tool Call"
          },
          "tool_kind": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ToolKind"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "request_id",
          "description",
          "options"
        ],
        "title": "PermissionSnapshot",
        "description": "Outstanding permission request in a state snapshot."
      },
      "PlanEntry": {
        "properties": {
          "content": {
            "type": "string",
            "title": "Content"
          },
          "status": {
            "type": "string",
            "title": "Status",
            "default": "pending"
          },
          "priority": {
            "type": "string",
            "title": "Priority",
            "default": "medium"
          }
        },
        "type": "object",
        "required": [
          "content"
        ],
        "title": "PlanEntry",
        "description": "A single entry in the agent's execution plan.\n\nFields use plain ``str`` so the domain layer stays free of wire-protocol\nenum imports.  Values correspond to ``PlanEntryStatus`` / ``PlanEntryPriority``\nmembers defined in ``api.schemas.enums``."
      },
      "PresetOrigin": {
        "type": "string",
        "enum": [
          "bundled",
          "workspace",
          "test_mock"
        ],
        "title": "PresetOrigin",
        "description": "Which source supplied a discovered preset.\n\n``WORKSPACE`` outranks ``BUNDLED`` for the same id, because a workspace\ndefinition shadows the shipped one; the origin therefore reports where the\npreset a caller would actually get was found, not merely where a file\nexists. ``TEST_MOCK`` is a shipped preset that follows the mock naming\nconvention, kept a separate member rather than folded into ``BUNDLED`` so\nthe product layer can exclude fixtures without matching on the id."
      },
      "PresetSummary": {
        "properties": {
          "id": {
            "type": "string",
            "title": "Id"
          },
          "loadable": {
            "type": "boolean",
            "title": "Loadable"
          },
          "unavailable_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Unavailable Reason"
          },
          "display_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Display Name"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "topology": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TopologyType"
              },
              {
                "type": "null"
              }
            ]
          },
          "worker_count": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Worker Count"
          },
          "required_roles": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Required Roles"
          },
          "authoring_capability": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/AuthoringCapability"
              },
              {
                "type": "null"
              }
            ]
          },
          "is_mock": {
            "type": "boolean",
            "title": "Is Mock",
            "default": false
          },
          "origin": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PresetOrigin"
              },
              {
                "type": "null"
              }
            ]
          },
          "supported_capabilities": {
            "items": {
              "$ref": "#/components/schemas/DocumentCapability"
            },
            "type": "array",
            "title": "Supported Capabilities"
          },
          "profiles": {
            "items": {
              "$ref": "#/components/schemas/ProfileSummary"
            },
            "type": "array",
            "title": "Profiles"
          },
          "default_profile_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Default Profile Id"
          }
        },
        "type": "object",
        "required": [
          "id",
          "loadable"
        ],
        "title": "PresetSummary",
        "description": "One discovered team preset and whether it is actually runnable.\n\nA preset whose TOML is missing or invalid is still listed with\n``loadable=False`` and an ``unavailable_reason`` so the Rust backend sees the\ntruthful set rather than a listing that omits or crashes on it. Descriptive\nfields are populated only when the preset loaded."
      },
      "PresetsListResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "presets": {
            "items": {
              "$ref": "#/components/schemas/PresetSummary"
            },
            "type": "array",
            "title": "Presets"
          }
        },
        "type": "object",
        "title": "PresetsListResponse",
        "description": "List of available team presets."
      },
      "ProfileSummary": {
        "properties": {
          "id": {
            "type": "string",
            "title": "Id"
          },
          "display_name": {
            "type": "string",
            "title": "Display Name",
            "default": ""
          },
          "description": {
            "type": "string",
            "title": "Description",
            "default": ""
          },
          "is_default": {
            "type": "boolean",
            "title": "Is Default",
            "default": false
          },
          "eligible": {
            "type": "boolean",
            "title": "Eligible",
            "default": false
          },
          "unavailable_reasons": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Unavailable Reasons"
          },
          "assignments": {
            "items": {
              "$ref": "#/components/schemas/RoleAssignmentSummary"
            },
            "type": "array",
            "title": "Assignments"
          }
        },
        "type": "object",
        "required": [
          "id"
        ],
        "title": "ProfileSummary",
        "description": "One selectable model profile for a preset.\n\nCarries the profile's identity, whether it is the default, its per-role\neffective assignments (resolved by the same shared resolver launch uses, so\npicker truth cannot drift from execution truth), and backend-computed\neligibility with safe reasons."
      },
      "Provider": {
        "type": "string",
        "enum": [
          "claude",
          "codex",
          "deterministic",
          "gemini",
          "kimi",
          "mock",
          "openai",
          "zai",
          "zhipu"
        ],
        "title": "Provider",
        "description": "Supported LLM providers."
      },
      "ProviderCatalogEntryResponse": {
        "properties": {
          "entry_id": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "pattern": "^[^\\x00-\\x1f\\x7f]+$",
            "title": "Entry Id"
          },
          "display_name": {
            "type": "string",
            "maxLength": 256,
            "minLength": 1,
            "title": "Display Name"
          },
          "description": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1024,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "capabilities": {
            "items": {
              "type": "string",
              "maxLength": 1024,
              "minLength": 1
            },
            "type": "array",
            "maxItems": 64,
            "title": "Capabilities"
          },
          "native_control_ids": {
            "items": {
              "type": "string",
              "maxLength": 128,
              "minLength": 1,
              "pattern": "^[^\\x00-\\x1f\\x7f]+$"
            },
            "type": "array",
            "maxItems": 32,
            "title": "Native Control Ids"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "entry_id",
          "display_name"
        ],
        "title": "ProviderCatalogEntryResponse"
      },
      "ProviderCatalogRecordResponse": {
        "properties": {
          "provider_id": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "pattern": "^[^\\x00-\\x1f\\x7f]+$",
            "title": "Provider Id"
          },
          "display_name": {
            "type": "string",
            "maxLength": 256,
            "minLength": 1,
            "title": "Display Name"
          },
          "execution_mode": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "pattern": "^[^\\x00-\\x1f\\x7f]+$",
            "title": "Execution Mode"
          },
          "health": {
            "$ref": "#/components/schemas/ProviderHealthResponse"
          },
          "catalog": {
            "$ref": "#/components/schemas/ProviderLaneCatalogResponse"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "provider_id",
          "display_name",
          "execution_mode",
          "health",
          "catalog"
        ],
        "title": "ProviderCatalogRecordResponse"
      },
      "ProviderCatalogResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "providers": {
            "items": {
              "$ref": "#/components/schemas/ProviderCatalogRecordResponse"
            },
            "type": "array",
            "maxItems": 128,
            "title": "Providers"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "ProviderCatalogResponse"
      },
      "ProviderCatalogSelection": {
        "properties": {
          "schema_version": {
            "type": "integer",
            "const": 1,
            "title": "Schema Version"
          },
          "provider_id": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "title": "Provider Id"
          },
          "execution_mode": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "title": "Execution Mode"
          },
          "catalog_revision": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "title": "Catalog Revision"
          },
          "entry_id": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "title": "Entry Id"
          },
          "controls": {
            "additionalProperties": {
              "type": "string",
              "maxLength": 512,
              "minLength": 1
            },
            "propertyNames": {
              "maxLength": 128,
              "minLength": 1
            },
            "type": "object",
            "maxProperties": 32,
            "title": "Controls"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "schema_version",
          "provider_id",
          "execution_mode",
          "catalog_revision",
          "entry_id"
        ],
        "title": "ProviderCatalogSelection",
        "description": "One explicit schema-v1 reference to a served provider catalog entry."
      },
      "ProviderCatalogStateResponse": {
        "properties": {
          "status": {
            "$ref": "#/components/schemas/CatalogStatus"
          },
          "checked_at": {
            "type": "string",
            "format": "date-time",
            "title": "Checked At"
          },
          "revision": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 512,
                "minLength": 1,
                "pattern": "^[^\\x00-\\x1f\\x7f]+$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revision"
          },
          "expires_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1024,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "status",
          "checked_at"
        ],
        "title": "ProviderCatalogStateResponse"
      },
      "ProviderCondition": {
        "type": "string",
        "enum": [
          "network_unreachable",
          "provider_overloaded",
          "unauthenticated",
          "throttled",
          "usage_exhausted",
          "credits_exhausted",
          "budget_exhausted",
          "invalid_request",
          "unknown"
        ],
        "title": "ProviderCondition",
        "description": "Why a provider failed, in terms a client can act on.\n\nThe value is the wire form: lowercase, underscore-separated, and stable."
      },
      "ProviderEligibility": {
        "type": "string",
        "enum": [
          "eligible",
          "ineligible"
        ],
        "title": "ProviderEligibility",
        "description": "Whether at least one subprocess provider can actually run on this host.\n\nComputed through the credential-aware readiness probe: the configured\ncredential is checked first, then command resolvability. The probe is still\nno-instantiation - no provider is constructed and no subprocess is spawned\nto determine it - but a resolvable launch command is not on its own\nsufficient, because a provider whose binary is installed with its\ncredential absent cannot run. Codex is the deliberate exception: its auth\nis a file-based persisted session rather than a configured secret, so it\ngates on command resolvability alone."
      },
      "ProviderHealthResponse": {
        "properties": {
          "configured": {
            "$ref": "#/components/schemas/HealthState"
          },
          "transport": {
            "$ref": "#/components/schemas/HealthState"
          },
          "authentication": {
            "$ref": "#/components/schemas/AuthenticationState"
          },
          "catalog": {
            "$ref": "#/components/schemas/CatalogStatus"
          },
          "admission": {
            "$ref": "#/components/schemas/AdmissionState"
          },
          "selectable": {
            "type": "boolean",
            "title": "Selectable"
          },
          "reasons": {
            "items": {
              "type": "string",
              "maxLength": 1024,
              "minLength": 1
            },
            "type": "array",
            "maxItems": 16,
            "title": "Reasons"
          },
          "checked_at": {
            "type": "string",
            "format": "date-time",
            "title": "Checked At"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "configured",
          "transport",
          "authentication",
          "catalog",
          "admission",
          "selectable",
          "checked_at"
        ],
        "title": "ProviderHealthResponse"
      },
      "ProviderLaneCatalogResponse": {
        "properties": {
          "schema_version": {
            "type": "integer",
            "const": 1,
            "title": "Schema Version",
            "default": 1
          },
          "state": {
            "$ref": "#/components/schemas/ProviderCatalogStateResponse"
          },
          "recommended_entry_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 512,
                "minLength": 1,
                "pattern": "^[^\\x00-\\x1f\\x7f]+$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Recommended Entry Id"
          },
          "models": {
            "items": {
              "$ref": "#/components/schemas/ProviderCatalogEntryResponse"
            },
            "type": "array",
            "maxItems": 256,
            "title": "Models"
          },
          "native_controls": {
            "items": {
              "$ref": "#/components/schemas/ProviderNativeControlResponse"
            },
            "type": "array",
            "maxItems": 32,
            "title": "Native Controls"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "state"
        ],
        "title": "ProviderLaneCatalogResponse"
      },
      "ProviderNativeControlOptionResponse": {
        "properties": {
          "option_id": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1,
            "pattern": "^[^\\x00-\\x1f\\x7f]+$",
            "title": "Option Id"
          },
          "display_name": {
            "type": "string",
            "maxLength": 256,
            "minLength": 1,
            "title": "Display Name"
          },
          "description": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1024,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "option_id",
          "display_name"
        ],
        "title": "ProviderNativeControlOptionResponse"
      },
      "ProviderNativeControlResponse": {
        "properties": {
          "control_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[^\\x00-\\x1f\\x7f]+$",
            "title": "Control Id"
          },
          "kind": {
            "$ref": "#/components/schemas/ControlKind"
          },
          "display_name": {
            "type": "string",
            "maxLength": 256,
            "minLength": 1,
            "title": "Display Name"
          },
          "options": {
            "items": {
              "$ref": "#/components/schemas/ProviderNativeControlOptionResponse"
            },
            "type": "array",
            "maxItems": 128,
            "title": "Options"
          },
          "default_option_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 512,
                "minLength": 1,
                "pattern": "^[^\\x00-\\x1f\\x7f]+$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Default Option Id"
          },
          "description": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 1024,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "control_id",
          "kind",
          "display_name"
        ],
        "title": "ProviderNativeControlResponse"
      },
      "RepairStatus": {
        "type": "string",
        "enum": [
          "healthy",
          "paused_resumable",
          "cancel_pending",
          "replay_gap",
          "checkpoint_unavailable",
          "needs_reconciliation",
          "operator_intervention_required"
        ],
        "title": "RepairStatus",
        "description": "Repair and readiness classification distinct from lifecycle.\n\nTypes BOTH durable columns that carry this classification:\n``threads.repair_status`` and ``threads.execution_readiness``. The two ask\ndifferent questions — what is wrong with this run, and is it fit to resume —\nbut they answer from this one closed set, so a new member becomes available\nto both at once and neither can drift into a private vocabulary."
      },
      "RoleAssignmentSummary": {
        "properties": {
          "role_id": {
            "type": "string",
            "title": "Role Id"
          },
          "agent_id": {
            "type": "string",
            "title": "Agent Id"
          },
          "provider_id": {
            "type": "string",
            "title": "Provider Id"
          },
          "capability": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Model"
              },
              {
                "type": "null"
              }
            ]
          },
          "model_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Model Name"
          },
          "fallback_providers": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Fallback Providers"
          },
          "provider_ready": {
            "type": "boolean",
            "title": "Provider Ready",
            "default": false
          },
          "source": {
            "$ref": "#/components/schemas/AssignmentSource",
            "default": "team_default"
          },
          "resolution_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Resolution Error"
          }
        },
        "type": "object",
        "required": [
          "role_id",
          "agent_id",
          "provider_id"
        ],
        "title": "RoleAssignmentSummary",
        "description": "Effective per-role model assignment under a profile.\n\nOnly safe operational metadata: role id, agent id, provider id, capability,\nthe stable concrete model name, ordered fallbacks, current provider readiness,\nand which precedence layer the assignment came from. Never a credential, env\nvalue, token, or private path.\n\n``provider_id`` is EMPTY when no preset layer declares a provider for the\nrole, which is the ordinary state now that product presets carry no\nprovider policy: a run chooses its lane at run start from the served catalog.\nEmpty rather than absent so the field keeps one type, and never a substituted\nprovider name - naming a lane no preset asked for would be indistinguishable\nfrom a real declaration. ``resolution_error`` carries the reason and ``source``\nreads ``undeclared``; the role is reported ineligible.\n\n``provider_id`` is deliberately NOT typed by the ``Provider`` enumeration\nserved beside it on the agent snapshot, even though the two carry the same\nconcept. This surface replays FROZEN historical assignments, and two of its\nvalues are provably outside that enumeration: the empty string documented\nabove, and a provider a past run froze that this build no longer knows -\na case the disclosure path already handles explicitly by reporting it not\nready rather than raising. Narrowing here would turn both into a validation\nfailure on a read path. The reconciliation is therefore that these are two\nconcepts: ``Provider`` is the closed set of lanes this build can RUN, and\nthis is an identifier a past run RECORDED."
      },
      "RoleState": {
        "properties": {
          "agent_id": {
            "type": "string",
            "title": "Agent Id"
          },
          "role": {
            "type": "string",
            "title": "Role",
            "default": ""
          },
          "state": {
            "type": "string",
            "title": "State"
          },
          "display_name": {
            "type": "string",
            "title": "Display Name",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "state"
        ],
        "title": "RoleState",
        "description": "Per-role lifecycle state within a run (recovery snapshot)."
      },
      "RunAdmission": {
        "type": "string",
        "enum": [
          "ready",
          "deferred",
          "blocked"
        ],
        "title": "RunAdmission",
        "description": "Whether the gateway would admit a run right now - the execution-ready fact.\n\n``ready`` means execution-ready: a reachable worker and a provider that is\nboth installed and credentialed, so this staged gate and the\ncredential-aware gate launch applies agree, rather than admitting a run and\nreserving capacity for it that the latter then refuses.\n``deferred`` means gateway-ready but not yet execution-ready - the worker is\ncold or starting and will start on demand. It remains informational on the\nreadiness surface, while staged ``prepare`` admission refuses it fail-closed.\n``blocked`` means a hard gateway dependency (the database) is unavailable."
      },
      "RunAgentSummary": {
        "properties": {
          "agent_id": {
            "type": "string",
            "title": "Agent Id"
          },
          "display_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Display Name"
          },
          "state": {
            "type": "string",
            "title": "State"
          }
        },
        "type": "object",
        "required": [
          "agent_id",
          "state"
        ],
        "title": "RunAgentSummary",
        "description": "One agent's disclosed operational state in the team projection."
      },
      "RunArchiveResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "$ref": "#/components/schemas/ThreadStatus",
            "default": "archived"
          }
        },
        "type": "object",
        "required": [
          "run_id"
        ],
        "title": "RunArchiveResponse",
        "description": "Acknowledge a run moved to the archived state."
      },
      "RunCancelResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "cancelled": {
            "type": "boolean",
            "title": "Cancelled"
          },
          "accepted": {
            "type": "boolean",
            "title": "Accepted",
            "default": false
          },
          "applied": {
            "type": "boolean",
            "title": "Applied",
            "default": false
          },
          "action_status": {
            "type": "string",
            "title": "Action Status",
            "default": "rejected_invalid_state"
          },
          "idempotency_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idempotency Key"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "status",
          "cancelled"
        ],
        "title": "RunCancelResponse",
        "description": "Acknowledge an (idempotent) run-cancel request."
      },
      "RunClarificationRespondRequest": {
        "properties": {
          "answers": {
            "anyOf": [
              {
                "patternProperties": {
                  "^[A-Za-z0-9_][A-Za-z0-9_.\\-]*$": {
                    "type": "string",
                    "maxLength": 2048
                  }
                },
                "propertyNames": {
                  "maxLength": 64,
                  "minLength": 1
                },
                "type": "object",
                "maxProperties": 4
              },
              {
                "type": "null"
              }
            ],
            "title": "Answers"
          },
          "prompt": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 65536,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Prompt"
          },
          "decline": {
            "anyOf": [
              {
                "type": "boolean",
                "const": true
              },
              {
                "type": "null"
              }
            ],
            "title": "Decline"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "RunClarificationRespondRequest",
        "description": "Resolve a parked questionnaire with answers, a new prompt, or a decline.\n\nCarries exactly one outcome: legacy answers keyed by question id, a new\nprompt that continues the same run, or a payload-free decline that lets the\nrun proceed with no answer given. The questions themselves were disclosed\nauthoritatively by ``run-status``; the run id and request id address the\nquestionnaire from the path, so no outcome restates what was asked.\n\nAll bounds are imported from the domain contract rather than restated. The\nchecks that need the parked questions in hand (an id nobody asked about, a\nrequired question left blank, a choice outside its declared options) are\napplied only to the answer outcome by the route against the checkpoint; a\ndecline deliberately bypasses them because refusal is not an answer.\n\n``decline`` admits only the literal ``true``: ``false`` would be a client\nsaying \"not declining\" while supplying no other outcome, which is a\ncontradiction better refused at the schema than interpreted."
      },
      "RunClarificationRespondResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "request_id": {
            "type": "string",
            "title": "Request Id"
          },
          "accepted": {
            "type": "boolean",
            "title": "Accepted"
          },
          "applied": {
            "type": "boolean",
            "title": "Applied",
            "default": false
          },
          "action_status": {
            "type": "string",
            "title": "Action Status"
          },
          "idempotency_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idempotency Key"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "request_id",
          "accepted",
          "action_status"
        ],
        "title": "RunClarificationRespondResponse",
        "description": "Report the durable outcome of a questionnaire resolution.\n\nIdentical retries replay the journal outcome without dispatch while a lease\nis fresh. ``applied`` becomes true only after the request-scoped checkpoint\nreceipt matches the accepted resolution fingerprint, including when a retry\nfollows a lost response after the graph already advanced."
      },
      "RunCommitResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "stage": {
            "type": "string",
            "const": "committed",
            "title": "Stage",
            "default": "committed"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "lease_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Lease Id"
          },
          "semantic_status": {
            "$ref": "#/components/schemas/SemanticPhase",
            "default": "starting"
          },
          "nickname": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Nickname"
          },
          "frozen_assignment": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FrozenTeamAssignmentSummary"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "status",
          "lease_id"
        ],
        "title": "RunCommitResponse",
        "description": "Acknowledge a run committed against a prepared reservation.\n\nReturned by the ``commit`` stage. The reservation is consumed and a stable\nrun is created and dispatched with the bound actor tokens. ``lease_id`` is the\nnon-secret, run-scoped lease identity the dashboard revokes at terminal\nsettlement; it is an identifier, never a bearer."
      },
      "RunDeleteResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "deleted": {
            "type": "boolean",
            "const": true,
            "title": "Deleted",
            "default": true
          },
          "cleanup_abandoned": {
            "type": "boolean",
            "const": true,
            "title": "Cleanup Abandoned",
            "default": true
          },
          "abandoned_kinds": {
            "items": {
              "$ref": "#/components/schemas/CleanupKind"
            },
            "type": "array",
            "minItems": 1,
            "title": "Abandoned Kinds"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "abandoned_kinds"
        ],
        "title": "RunDeleteResponse",
        "description": "Report a deletion that finalized over state no pass could remove.\n\nCarried only on that one outcome. A clean deletion answers with no body at\nall, so the presence of this body IS the signal that external state was\nstranded. Names the KINDS of item left behind and nothing more: a concrete\ncheckpoint id or artifact path is control-plane state and never the\ncaller's to receive."
      },
      "RunHistoryResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "state": {
            "$ref": "#/components/schemas/ThreadStateSnapshot"
          },
          "metadata": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ThreadMetadata"
              },
              {
                "type": "null"
              }
            ]
          },
          "transcript_available": {
            "type": "boolean",
            "title": "Transcript Available"
          },
          "transcript_status": {
            "$ref": "#/components/schemas/TranscriptAvailability"
          },
          "permission_decisions": {
            "items": {
              "$ref": "#/components/schemas/RunPermissionDecision"
            },
            "type": "array",
            "title": "Permission Decisions"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "state",
          "transcript_available",
          "transcript_status"
        ],
        "title": "RunHistoryResponse",
        "description": "The full read of one run, including terminal and archived ones.\n\nDeliberately distinct from run-status, which is the BOUNDED recovery\nsnapshot an engine reconciles from. This is the wide read - transcript,\nagents, plan, pending answers, and the run's metadata - for a consumer that\nwants the whole record rather than the authority fields.\n\nThe state snapshot is embedded by reference rather than restated field by\nfield, so the two cannot drift apart as the snapshot evolves.\n\nThe transcript is the one part of the record this verb cannot always\ndeliver, because it lives only in the checkpoint. ``transcript_available``\nand ``transcript_status`` say so outright, so ``state.messages`` is never\nread as \"this run had no conversation\" when the truth is that its\nconversation could not be read. They are the transcript's counterpart to the\nsnapshot's own ``snapshot_complete`` / ``degraded_reasons`` pairing.\n\n``permission_decisions`` is the settled counterpart to the snapshot's PENDING\npermissions. A gate leaves the pending list the moment it is answered or the\nrun ends, so without this the record of a decision a human actually made was\ndurable in the audit log and readable nowhere - a run could be reviewed whole\nwith no trace that anyone had approved anything."
      },
      "RunMessageRequest": {
        "properties": {
          "content": {
            "type": "string",
            "maxLength": 65536,
            "minLength": 1,
            "title": "Content"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "content"
        ],
        "title": "RunMessageRequest",
        "description": "Send a follow-up turn into a run that already exists.\n\nRun-start cannot express this: a repeat run identifier is a REPLAY, answered\nwith the original run, so there is no way to say \"same run, new input\"\nthrough it. This is that verb."
      },
      "RunMessageResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "accepted": {
            "type": "boolean",
            "title": "Accepted",
            "default": true
          },
          "applied": {
            "type": "boolean",
            "title": "Applied",
            "default": false
          },
          "action_status": {
            "type": "string",
            "title": "Action Status"
          },
          "action_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Action Id"
          },
          "idempotency_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idempotency Key"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "action_status"
        ],
        "title": "RunMessageResponse",
        "description": "Acknowledge a follow-up turn as accepted for dispatch.\n\nAcceptance is not completion: the turn is handed to the worker and the run\ncontinues asynchronously, so a caller reconciles progress from the stream or\nrun-status rather than from this body."
      },
      "RunPendingPermission": {
        "properties": {
          "request_id": {
            "type": "string",
            "title": "Request Id"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "request_status": {
            "type": "string",
            "title": "Request Status"
          }
        },
        "type": "object",
        "required": [
          "request_id",
          "run_id",
          "request_status"
        ],
        "title": "RunPendingPermission",
        "description": "A permission awaiting an answer, addressed by the run that raised it."
      },
      "RunPermissionDecision": {
        "properties": {
          "tool_name": {
            "type": "string",
            "title": "Tool Name"
          },
          "action": {
            "type": "string",
            "title": "Action"
          },
          "option_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Option Id"
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "responded_at": {
            "type": "string",
            "format": "date-time",
            "title": "Responded At"
          }
        },
        "type": "object",
        "required": [
          "tool_name",
          "action",
          "responded_at"
        ],
        "title": "RunPermissionDecision",
        "description": "One permission decision this run settled, as the audit log recorded it.\n\nAnswers which tool call was approved or refused, with which option, and when.\nCarries no tool INPUT and no prompt text, so a caller reviewing what a run was\npermitted to do never has to read what it was asked to do.\n\n``agent_id`` is nullable because it is genuinely not knowable at the decision\nseam rather than merely unset: the interrupt payload carries the tool, its\ninput, and the options, but no agent. An unattributed decision is still a real\nrecord, and the alternative is fabricating attribution."
      },
      "RunPermissionRespondRequest": {
        "properties": {
          "option_id": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Option Id"
          },
          "notes": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 2048
              },
              {
                "type": "null"
              }
            ],
            "title": "Notes"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "option_id"
        ],
        "title": "RunPermissionRespondRequest",
        "description": "Answer one permission request raised by a run.\n\nCarries the chosen option and, optionally, a reviewer comment. The options\nthemselves were advertised on the versioned progress stream in the\n``permission_request`` frame that raised the question, so the answer names\none rather than restating it. ``notes`` survives into the verdict resume\npayload for a locally-respondable verdict-style pause (D6); it is ignored\nfor a plain tool-permission response, which resumes on the bare option id."
      },
      "RunPermissionRespondResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "request_id": {
            "type": "string",
            "title": "Request Id"
          },
          "accepted": {
            "type": "boolean",
            "title": "Accepted"
          },
          "applied": {
            "type": "boolean",
            "title": "Applied"
          },
          "action_status": {
            "type": "string",
            "title": "Action Status"
          },
          "approval_status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Approval Status"
          },
          "idempotency_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idempotency Key"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "request_id",
          "accepted",
          "applied",
          "action_status"
        ],
        "title": "RunPermissionRespondResponse",
        "description": "Report what an answer did, including when it did nothing.\n\n``accepted`` says the answer was taken; ``applied`` reports whether the\nworker had durably confirmed applying this request's resolution when this\nresponse was assembled - never whether THIS call resumed the run. A first\naccepted answer therefore returns ``applied`` false with ``action_status``\n``accepted_not_applied``: the resume is dispatched and application is\nconfirmed asynchronously. A duplicate replays the stored outcome rather\nthan acting twice - byte-identical to the first response until that\nconfirmation lands, ``applied`` true after it - so a caller retrying after\na lost response learns the current outcome instead of re-answering. An\nanswer arriving after the request was already applied reports ``accepted``\nwith ``applied`` true and ``action_status`` ``duplicate``."
      },
      "RunPrepareResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "stage": {
            "type": "string",
            "const": "prepared",
            "title": "Stage",
            "default": "prepared"
          },
          "reservation_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Reservation Id"
          },
          "lease_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Lease Id"
          },
          "required_roles": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 64,
            "title": "Required Roles"
          },
          "expires_at": {
            "type": "string",
            "title": "Expires At"
          },
          "worker_state": {
            "$ref": "#/components/schemas/WorkerLifecycleState"
          },
          "provider_eligibility": {
            "$ref": "#/components/schemas/ProviderEligibility"
          },
          "run_admission": {
            "$ref": "#/components/schemas/RunAdmission"
          },
          "reasons": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "maxItems": 16,
            "title": "Reasons"
          }
        },
        "type": "object",
        "required": [
          "reservation_id",
          "lease_id",
          "expires_at",
          "worker_state",
          "provider_eligibility",
          "run_admission"
        ],
        "title": "RunPrepareResponse",
        "description": "Acknowledge a prepared admission reservation.\n\nReturned by the ``prepare`` stage of run-start. It carries the server-minted\nreservation identity a later ``commit`` binds to, its non-secret run-scoped\nlease identity, the bounded validated set of roles that commit's actor-token\nbundle must cover, and the reservation's hard expiry. The three readiness\nfacts report why admission is or is not\nexecution-ready right now. No durable run exists yet and no token was\naccepted; a reservation that is never committed simply expires."
      },
      "RunReleaseResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "stage": {
            "type": "string",
            "const": "released",
            "title": "Stage",
            "default": "released"
          },
          "reservation_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Reservation Id"
          },
          "released": {
            "type": "boolean",
            "title": "Released"
          }
        },
        "type": "object",
        "required": [
          "reservation_id",
          "released"
        ],
        "title": "RunReleaseResponse",
        "description": "Acknowledge explicit release of an uncommitted reservation."
      },
      "RunStage": {
        "type": "string",
        "enum": [
          "start",
          "prepare",
          "commit",
          "release"
        ],
        "title": "RunStage",
        "description": "Which stage of the run-start verb a request drives.\n\nThe single ``POST /v1/runs`` verb supports ``prepare`` for readiness-gated,\nexpiring capacity; ``commit`` for exact request and actor-role binding;\n``release`` for an uncommitted reservation; and the pre-existing one-shot\n``start`` compatibility path."
      },
      "RunStartRequest": {
        "properties": {
          "stage": {
            "$ref": "#/components/schemas/RunStage",
            "default": "start"
          },
          "reservation_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128,
                "minLength": 1,
                "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reservation Id"
          },
          "team_preset": {
            "type": "string",
            "maxLength": 64,
            "minLength": 1,
            "title": "Team Preset"
          },
          "message": {
            "type": "string",
            "maxLength": 65536,
            "title": "Message",
            "default": ""
          },
          "actor_tokens": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ActorTokenBundle"
              },
              {
                "type": "null"
              }
            ]
          },
          "metadata": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ThreadMetadata"
              },
              {
                "type": "null"
              }
            ]
          },
          "autonomous": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Autonomous"
          },
          "title": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 200
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "feature_tag": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128
              },
              {
                "type": "null"
              }
            ],
            "title": "Feature Tag"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "selection": {
            "$ref": "#/components/schemas/ProviderCatalogSelection"
          },
          "overrides": {
            "additionalProperties": {
              "$ref": "#/components/schemas/ProviderCatalogSelection"
            },
            "propertyNames": {
              "maxLength": 63,
              "minLength": 1
            },
            "type": "object",
            "maxProperties": 64,
            "title": "Overrides"
          },
          "fallbacks": {
            "items": {
              "$ref": "#/components/schemas/ProviderCatalogSelection"
            },
            "type": "array",
            "maxItems": 8,
            "title": "Fallbacks"
          },
          "feedback_batch_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256,
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Feedback Batch Id"
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "team_preset",
          "run_id",
          "selection"
        ],
        "title": "RunStartRequest",
        "description": "Start a run: the engine-facing shape of thread-create + first message.\n\nCarries the engine-provisioned per-role actor token bundle alongside\nthe run's preset and opening message. The gateway threads this onto the same\ndispatch path the internal thread-create flow uses — no second code path."
      },
      "RunStartResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "nickname": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Nickname"
          },
          "semantic_status": {
            "$ref": "#/components/schemas/SemanticPhase",
            "default": "starting"
          },
          "eligible": {
            "type": "boolean",
            "title": "Eligible",
            "default": true
          },
          "frozen_assignment": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FrozenTeamAssignmentSummary"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "status"
        ],
        "title": "RunStartResponse",
        "description": "Acknowledge a started run, with its initial semantic status."
      },
      "RunStatusResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "$ref": "#/components/schemas/ThreadStatus"
          },
          "semantic_phase": {
            "$ref": "#/components/schemas/SemanticPhase"
          },
          "feature_tag": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Feature Tag"
          },
          "authoring_session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Authoring Session Id"
          },
          "topology": {
            "$ref": "#/components/schemas/TopologyPosition"
          },
          "roles": {
            "items": {
              "$ref": "#/components/schemas/RoleState"
            },
            "type": "array",
            "title": "Roles"
          },
          "proposal_ids": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Proposal Ids"
          },
          "changeset_ids": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Changeset Ids"
          },
          "approval_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ApprovalStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "approval_request_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Approval Request Id"
          },
          "checkpoint_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Id"
          },
          "last_sequence": {
            "type": "integer",
            "title": "Last Sequence",
            "default": 0
          },
          "repair_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RepairStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "execution_readiness": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RepairStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "degraded_reasons": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Degraded Reasons"
          },
          "failure_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Failure Reason"
          },
          "provider_condition": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ProviderCondition"
              },
              {
                "type": "null"
              }
            ]
          },
          "repair_reason": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 500
              },
              {
                "type": "null"
              }
            ],
            "title": "Repair Reason"
          },
          "profile_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Profile Id"
          },
          "assignments": {
            "items": {
              "$ref": "#/components/schemas/RoleAssignmentSummary"
            },
            "type": "array",
            "title": "Assignments"
          },
          "frozen_assignment": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/FrozenTeamAssignmentSummary"
              },
              {
                "type": "null"
              }
            ]
          },
          "lease_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128,
                "minLength": 1,
                "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Lease Id"
          },
          "reservation_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128,
                "minLength": 1,
                "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reservation Id"
          },
          "pending_clarification": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ClarificationRequest"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "status",
          "semantic_phase",
          "topology"
        ],
        "title": "RunStatusResponse",
        "description": "The authoritative recovery snapshot for a run.\n\nDesigned as the read a restarted A2A resumes or reports a run from: topology\nposition, per-role state, and the engine proposal/changeset ids the run has\nproduced, plus the checkpoint cursor and repair posture. Non-authoritative\nSSE progress frames may be lost freely; this snapshot is the source of truth."
      },
      "RunSummariesResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "state": {
            "type": "string",
            "const": "all",
            "title": "State",
            "default": "all"
          },
          "runs": {
            "items": {
              "$ref": "#/components/schemas/RunSummaryRecord"
            },
            "type": "array",
            "maxItems": 100,
            "title": "Runs"
          },
          "truncated": {
            "type": "boolean",
            "title": "Truncated",
            "default": false
          },
          "total": {
            "type": "integer",
            "title": "Total"
          }
        },
        "type": "object",
        "required": [
          "total"
        ],
        "title": "RunSummariesResponse",
        "description": "The history reading of the run listing.\n\nAnswers ``state=all`` only. The default reading keeps\n:class:`ActiveRunsResponse` and its narrower record, so widening what history\nreports cannot disturb the certified discovery shape.\n\n``total`` is not optional here: this reading walks the paginated store and\nalways knows how many runs matched, which is what makes paging honest."
      },
      "RunSummaryRecord": {
        "properties": {
          "run_id": {
            "type": "string",
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9_][A-Za-z0-9_-]{0,127}$",
            "title": "Run Id"
          },
          "status": {
            "$ref": "#/components/schemas/ThreadStatus"
          },
          "feature_tag": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128
              },
              {
                "type": "null"
              }
            ],
            "title": "Feature Tag"
          },
          "title": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 200
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "nickname": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128
              },
              {
                "type": "null"
              }
            ],
            "title": "Nickname"
          },
          "team_preset": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 64
              },
              {
                "type": "null"
              }
            ],
            "title": "Team Preset"
          },
          "repair_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RepairStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "execution_readiness": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RepairStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "approval_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ApprovalStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "approval_request_id": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Approval Request Id"
          },
          "created_at": {
            "type": "string",
            "format": "date-time",
            "title": "Created At"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time",
            "title": "Updated At"
          },
          "source_branch": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 256
              },
              {
                "type": "null"
              }
            ],
            "title": "Source Branch"
          },
          "callee": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 128
              },
              {
                "type": "null"
              }
            ],
            "title": "Callee"
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "status",
          "created_at",
          "updated_at"
        ],
        "title": "RunSummaryRecord",
        "description": "One run as the history reading reports it: identity plus its projection.\n\nDeliberately wider than :class:`ActiveRunRecord`, and deliberately a\nSEPARATE model rather than fields added to it. Discovery answers the narrow\nquestion \"which runs are live, and where do I bind a viewer\" and its shape is\ncertified byte-for-byte; a reader of history is asking what happened, and\nneeds the facts that distinguish a healthy run from a degraded one.\n\nEvery field here is one the list service already resolves - the degradation\nprojections in particular are its reading of checkpoint authority and stale\napproval pointers, not a restatement of stored columns - so nothing is\ninvented that the projection cannot fill."
      },
      "SemanticPhase": {
        "type": "string",
        "enum": [
          "starting",
          "running",
          "researching",
          "synthesizing_research",
          "reviewing_research",
          "awaiting_research_decision",
          "writing_adr",
          "reviewing_adr",
          "awaiting_adr_decision",
          "writing_plan",
          "reviewing_plan",
          "awaiting_plan_decision",
          "recovery_required",
          "completed",
          "failed",
          "cancelled"
        ],
        "title": "SemanticPhase",
        "description": "What a run is product-visibly doing, in terms that name no graph node.\n\nThe one vocabulary for a run's semantic position, served on run-status as\n``semantic_phase``, on the run-start and commit acknowledgements as\n``semantic_status``, and stamped onto progress frames. Those three fields\nask the same question at different moments, so they answer from this set.\n\nIt is deliberately wider than the authoring phases alone. A run outside the\nresearch_adr topology, or between nodes, is honestly ``RUNNING`` rather than\ngiven a fabricated authoring phase, and a run that has not dispatched is\n``STARTING``. The terminal members collapse the lifecycle pairs a product\nreader cannot act on separately: an archived run reads ``COMPLETED`` and a\ncancelling one reads ``CANCELLED``, because the distinction is a lifecycle\nfact and this vocabulary is a product one.\n\n``RECOVERY_REQUIRED`` is the only member that is not a position: it says the\nrun cannot advance until it is repaired, which is what a reader needs before\nany phase detail matters."
      },
      "ServiceStateResponse": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "service_version": {
            "type": "string",
            "title": "Service Version"
          },
          "status": {
            "type": "string",
            "title": "Status"
          },
          "alive": {
            "type": "boolean",
            "title": "Alive",
            "default": true
          },
          "ready": {
            "type": "boolean",
            "title": "Ready"
          },
          "can_accept_run": {
            "type": "boolean",
            "title": "Can Accept Run"
          },
          "gateway_pid": {
            "type": "integer",
            "title": "Gateway Pid"
          },
          "gateway_lifetime_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Gateway Lifetime Id"
          },
          "worker_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/WorkerConnectionStatus"
              },
              {
                "type": "null"
              }
            ]
          },
          "worker_connected": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Worker Connected"
          },
          "worker_paired_gateway_lifetime": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Worker Paired Gateway Lifetime"
          },
          "worker_generation": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Worker Generation"
          },
          "circuit_breaker": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Circuit Breaker"
          },
          "database_backend": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Database Backend"
          },
          "checkpoint_backend": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Backend"
          },
          "database_ready": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Database Ready"
          },
          "checkpoint_ready": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Ready"
          },
          "worker_ready": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Worker Ready"
          },
          "authoring_backend_reachable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Authoring Backend Reachable"
          },
          "active_run_capacity": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Active Run Capacity"
          },
          "degraded_reasons": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Degraded Reasons"
          },
          "routes": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Routes"
          },
          "readiness": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DesktopReadiness"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "type": "object",
        "required": [
          "service_version",
          "status",
          "ready",
          "can_accept_run",
          "gateway_pid"
        ],
        "title": "ServiceStateResponse",
        "description": "Backend-served readiness for the A2A service (service-state verb).\n\nDistinguishes three truths the Rust backend must not conflate: the process is\n``alive`` (it answered), the service ``can_accept_run`` (dependencies are\nready), and - separately, via presets-list eligibility - whether a chosen\nauthoring preset is runnable. A live HTTP process is not evidence that a run\ncan start, so ``status`` is derived from real dependency probes rather than\nhardcoded."
      },
      "TeamStatusV1Response": {
        "properties": {
          "api_version": {
            "type": "string",
            "const": "v1",
            "title": "Api Version",
            "default": "v1"
          },
          "agents": {
            "items": {
              "$ref": "#/components/schemas/RunAgentSummary"
            },
            "type": "array",
            "title": "Agents"
          },
          "active_runs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Active Runs"
          },
          "pending_permissions": {
            "items": {
              "$ref": "#/components/schemas/RunPendingPermission"
            },
            "type": "array",
            "title": "Pending Permissions"
          }
        },
        "type": "object",
        "title": "TeamStatusV1Response",
        "description": "The team's live operational projection.\n\nSafe operational metadata only - which agents exist, what state they are in,\nwhich runs are active, and what is awaiting an answer. Never a credential,\na prompt, or a document body."
      },
      "ThreadMetadata": {
        "properties": {
          "nickname": {
            "type": "string",
            "title": "Nickname",
            "default": ""
          },
          "workspace_root": {
            "type": "string",
            "title": "Workspace Root"
          },
          "source_repo": {
            "type": "string",
            "title": "Source Repo",
            "default": ""
          },
          "source_branch": {
            "type": "string",
            "title": "Source Branch",
            "default": ""
          },
          "callee": {
            "type": "string",
            "title": "Callee",
            "default": ""
          },
          "feature_tag": {
            "type": "string",
            "title": "Feature Tag",
            "default": ""
          },
          "context_refs": {
            "items": {
              "$ref": "#/components/schemas/ContextRef"
            },
            "type": "array",
            "title": "Context Refs"
          },
          "feedback_batch_id": {
            "type": "string",
            "title": "Feedback Batch Id",
            "default": ""
          }
        },
        "type": "object",
        "required": [
          "workspace_root"
        ],
        "title": "ThreadMetadata",
        "description": "Provenance and context attached to an orchestration thread."
      },
      "ThreadStateSnapshot": {
        "properties": {
          "thread_id": {
            "type": "string",
            "title": "Thread Id"
          },
          "status": {
            "$ref": "#/components/schemas/ThreadStatus"
          },
          "messages": {
            "items": {
              "$ref": "#/components/schemas/MessageSnapshot"
            },
            "type": "array",
            "title": "Messages"
          },
          "tool_calls": {
            "items": {
              "$ref": "#/components/schemas/ToolCallSnapshot"
            },
            "type": "array",
            "title": "Tool Calls"
          },
          "pending_permissions": {
            "items": {
              "$ref": "#/components/schemas/PermissionSnapshot"
            },
            "type": "array",
            "title": "Pending Permissions"
          },
          "pending_clarification": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ClarificationRequestSnapshot"
              },
              {
                "type": "null"
              }
            ]
          },
          "artifacts": {
            "items": {
              "$ref": "#/components/schemas/ArtifactSnapshot"
            },
            "type": "array",
            "title": "Artifacts"
          },
          "plan": {
            "items": {
              "$ref": "#/components/schemas/PlanEntry"
            },
            "type": "array",
            "title": "Plan"
          },
          "agents": {
            "items": {
              "$ref": "#/components/schemas/AgentSnapshot"
            },
            "type": "array",
            "title": "Agents"
          },
          "last_sequence": {
            "type": "integer",
            "title": "Last Sequence"
          },
          "checkpoint_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Id"
          },
          "checkpoint_created_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Created At"
          },
          "checkpoint_parent_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Parent Id"
          },
          "checkpoint_source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Source"
          },
          "checkpoint_step": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Checkpoint Step"
          },
          "checkpoint_updated_channels": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Checkpoint Updated Channels"
          },
          "pending_write_channels": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Pending Write Channels"
          },
          "pending_write_count": {
            "type": "integer",
            "title": "Pending Write Count",
            "default": 0
          },
          "history_depth": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "History Depth"
          },
          "next_nodes": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Next Nodes"
          },
          "task_count": {
            "type": "integer",
            "title": "Task Count",
            "default": 0
          },
          "pending_interrupt_count": {
            "type": "integer",
            "title": "Pending Interrupt Count",
            "default": 0
          },
          "execution_tasks": {
            "items": {
              "$ref": "#/components/schemas/ExecutionTaskSnapshot"
            },
            "type": "array",
            "title": "Execution Tasks"
          },
          "snapshot_complete": {
            "type": "boolean",
            "title": "Snapshot Complete",
            "default": true
          },
          "degraded_reasons": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Degraded Reasons"
          },
          "replay_status": {
            "type": "string",
            "title": "Replay Status",
            "default": "unknown"
          },
          "repair_status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Repair Status"
          },
          "execution_readiness": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Execution Readiness"
          },
          "pause_cause": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pause Cause"
          },
          "approval_status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Approval Status"
          },
          "approval_request_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Approval Request Id"
          },
          "failure_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Failure Reason"
          },
          "provider_condition": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Provider Condition"
          },
          "repair_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Repair Reason"
          }
        },
        "type": "object",
        "required": [
          "thread_id",
          "status",
          "last_sequence"
        ],
        "title": "ThreadStateSnapshot",
        "description": "Complete thread state for reconnection replay.\n\nThe client fetches this via REST, notes ``last_sequence``, then\ndiscards any WebSocket events with ``sequence <= last_sequence``."
      },
      "ThreadStatus": {
        "type": "string",
        "enum": [
          "submitted",
          "running",
          "input_required",
          "cancelling",
          "cancelled",
          "completed",
          "failed",
          "archived",
          "repair_needed",
          "reconciling",
          "deleting"
        ],
        "title": "ThreadStatus",
        "description": "Durable lifecycle states for orchestration threads."
      },
      "ToolCallContentDiff": {
        "properties": {
          "content_type": {
            "type": "string",
            "const": "diff",
            "title": "Content Type",
            "default": "diff"
          },
          "path": {
            "type": "string",
            "title": "Path"
          },
          "old_text": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Old Text"
          },
          "new_text": {
            "type": "string",
            "title": "New Text"
          }
        },
        "type": "object",
        "required": [
          "path",
          "new_text"
        ],
        "title": "ToolCallContentDiff",
        "description": "Diff content block within a tool call."
      },
      "ToolCallContentTerminal": {
        "properties": {
          "content_type": {
            "type": "string",
            "const": "terminal",
            "title": "Content Type",
            "default": "terminal"
          },
          "terminal_id": {
            "type": "string",
            "title": "Terminal Id"
          }
        },
        "type": "object",
        "required": [
          "terminal_id"
        ],
        "title": "ToolCallContentTerminal",
        "description": "Terminal output content block within a tool call."
      },
      "ToolCallContentText": {
        "properties": {
          "content_type": {
            "type": "string",
            "const": "text",
            "title": "Content Type",
            "default": "text"
          },
          "text": {
            "type": "string",
            "title": "Text"
          }
        },
        "type": "object",
        "required": [
          "text"
        ],
        "title": "ToolCallContentText",
        "description": "Plain text content block within a tool call."
      },
      "ToolCallLocation": {
        "properties": {
          "path": {
            "type": "string",
            "title": "Path"
          },
          "line": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Line"
          }
        },
        "type": "object",
        "required": [
          "path"
        ],
        "title": "ToolCallLocation",
        "description": "File location associated with a tool call."
      },
      "ToolCallSnapshot": {
        "properties": {
          "tool_call_id": {
            "type": "string",
            "title": "Tool Call Id"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "kind": {
            "$ref": "#/components/schemas/ToolKind"
          },
          "status": {
            "$ref": "#/components/schemas/ToolCallStatus"
          },
          "locations": {
            "items": {
              "$ref": "#/components/schemas/ToolCallLocation"
            },
            "type": "array",
            "title": "Locations"
          },
          "content": {
            "items": {
              "oneOf": [
                {
                  "$ref": "#/components/schemas/ToolCallContentText"
                },
                {
                  "$ref": "#/components/schemas/ToolCallContentDiff"
                },
                {
                  "$ref": "#/components/schemas/ToolCallContentTerminal"
                }
              ],
              "discriminator": {
                "propertyName": "content_type",
                "mapping": {
                  "diff": "#/components/schemas/ToolCallContentDiff",
                  "terminal": "#/components/schemas/ToolCallContentTerminal",
                  "text": "#/components/schemas/ToolCallContentText"
                }
              }
            },
            "type": "array",
            "title": "Content"
          }
        },
        "type": "object",
        "required": [
          "tool_call_id",
          "title",
          "kind",
          "status"
        ],
        "title": "ToolCallSnapshot",
        "description": "Fully materialized tool call (all incremental updates merged)."
      },
      "ToolCallStatus": {
        "type": "string",
        "enum": [
          "pending",
          "in_progress",
          "completed",
          "failed"
        ],
        "title": "ToolCallStatus",
        "description": "Lifecycle states for a single tool invocation."
      },
      "ToolKind": {
        "type": "string",
        "enum": [
          "read",
          "edit",
          "delete",
          "move",
          "search",
          "execute",
          "think",
          "fetch",
          "switch_mode",
          "other"
        ],
        "title": "ToolKind",
        "description": "ACP tool categories (mirrors agentclientprotocol.com schema)."
      },
      "TopologyPosition": {
        "properties": {
          "team_preset": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Team Preset"
          },
          "active_agent": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Active Agent"
          },
          "pause_cause": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Pause Cause"
          }
        },
        "type": "object",
        "title": "TopologyPosition",
        "description": "Where a run sits in its team topology (recovery snapshot).\n\nProduct-facing status speaks role vocabulary only: ``active_agent`` is\nthe role currently working, never an internal LangGraph node name. The raw\nnext-node projection lives in the internal recovery snapshot\n(``thread/snapshots.py``), not this contract; ``next_nodes`` was dropped\nfrom the v1 surface."
      },
      "TopologyType": {
        "type": "string",
        "enum": [
          "star",
          "pipeline",
          "pipeline_loop",
          "research_adr"
        ],
        "title": "TopologyType",
        "description": "Supported graph topology types.\n\nUses enum membership instead of string comparison."
      },
      "TranscriptAvailability": {
        "type": "string",
        "enum": [
          "available",
          "not_yet_recorded",
          "missing",
          "unreadable"
        ],
        "title": "TranscriptAvailability",
        "description": "Why a run's conversation is, or is not, readable from its checkpoint.\n\nA run's transcript lives only in the checkpoint, so an unread checkpoint\nyields an empty message list that is indistinguishable from a run that\ngenuinely said nothing. This classification is what tells the two apart, and\nit exists so no reader has to infer loss from an empty list.\n\n``NOT_YET_RECORDED`` is the only non-fault absence: the run has not been\ndispatched, so there is nothing to have lost. ``MISSING`` and ``UNREADABLE``\nare both faults and differ in whether the checkpoint is gone or merely\nunreachable right now.\n\nA run whose transcript was deliberately released under a retention policy\nwould be a fourth, non-fault member. It is deliberately absent: no\nproduction seam prunes a checkpoint today, so every absence really is a\nfault, and a member with no producer would let a genuine loss be dismissed\nas intended."
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      },
      "WorkerConnectionStatus": {
        "type": "string",
        "enum": [
          "pending",
          "up",
          "down",
          "restarting",
          "unknown"
        ],
        "title": "WorkerConnectionStatus",
        "description": "What the watchdog last observed about the worker process it supervises.\n\n``PENDING`` is the pre-observation value the state container is created\nwith: a watchdog exists but has not yet completed a probe, which is not the\nsame claim as ``DOWN``. ``UP`` and ``DOWN`` are settled observations,\n``RESTARTING`` is the window in which the watchdog is deliberately replacing\nthe process, and ``UNKNOWN`` is what a reader gets when no worker state\ncontainer exists at all - a gateway configured without a supervised worker."
      },
      "WorkerLifecycleState": {
        "type": "string",
        "enum": [
          "cold",
          "starting",
          "ready",
          "unavailable"
        ],
        "title": "WorkerLifecycleState",
        "description": "The gateway-owned worker's rung on the cold-to-execution ladder.\n\n``cold`` is the pre-demand resting state: no worker exists yet and one is\nstartable on first execution demand. It is informational, never degradation.\n``starting`` is the single-flight startup window, ``ready`` is an up and\nreachable worker, and ``unavailable`` is a worker that started but is down or\nrestarting after demand."
      }
    },
    "securitySchemes": {
      "GatewayServiceToken": {
        "type": "http",
        "description": "Gateway service token. The service publishes it in an owner-restricted `service.token` handoff file adjacent to the `service.json` discovery record, whose `handoff_reference` names that file without embedding the secret. Send it as `Authorization: Bearer <token>`.",
        "scheme": "bearer"
      },
      "InternalIpcToken": {
        "type": "http",
        "description": "Internal gateway-to-worker IPC token. Not the gateway service token: these routes are the worker's callback surface, not a client surface.",
        "scheme": "bearer"
      }
    }
  }
}
