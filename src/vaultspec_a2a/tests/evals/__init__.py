"""Behavioural evaluation suite for Vaultspec A2A agents (ADR-027 Layer 3).

This package is NOT part of the production runtime. It requires the ``eval``
optional dependency group::

    uv sync --extra eval
"""
